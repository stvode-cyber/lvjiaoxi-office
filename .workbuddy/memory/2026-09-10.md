# 2026-09-10

## 决策
- **内测仅面向 Windows**：`release.yml`(0-job) / iOS / Android 的 CI 失败均为预存在问题，**不在内测范畴，挂起**，不排障。

## 业务板块深化：审批意见留痕
- 新增 `approvals.setStatus(id, status, comment)`：通过可填意见（可留空）、**驳回必填理由**（否则拒绝，仍为待审批）；历史 `statusHistory` 条带 `comment` 留痕。
- `update()` 也支持携带 comment 写历史。
- `detailData` 历史回显 `comment`；详情时间线展示意见（`状态 · 时间 · “意见”`）。
- UI：通过/驳回按钮走 `prompt` 收集意见（驳回留空给 toast 提示）。
- 新增 `_approval_comment_test.js` 18 断言。
- 门禁：**110 套件 0 失败** + 四端同源（sync-clients 同步 approvals.js）。

## CI 排查收口（桌面构建复绿）
- 根因修复：空证书注入崩溃 → `build-desktop`/`release.yml` 仅当提供签名 secret 时注入证书（`9569e74`）。
- 推送 `9569e74` 至 GitHub `main`（此前因 PAT 缺 `workflow` scope 被拒）。
- **Build Desktop (Electron) 验证 = success**（run 34425557655）。

## 凭证就绪（HTTPS 推送）
- 新 PAT 含 `repo`+`workflow`，存放：`C:\Users\Administrator\.lvjiaoxi-git-credentials`（仓库外）。
- **坑**：git HTTPS 推送须用 Basic/URL 内嵌 token（`Authorization: token` scheme 不被 git 接收→invalid credentials）。
- token 明文只在生成瞬间显示，及时抓取；生成页 scope 父子自动合并会弹提示，不影响 repo/wokflow 保留。

## Git remote
- remote 已从 SSH 切到 HTTPS `https://github.com/stvode-cyber/lvjiaoxi-office.git`。
- 上次会话的 SSH `Permission denied (publickey)` 已绕开；本地与远端 `main` 一致（`9569e74`，SYNCED）。

## 遗留（非内测范畴，挂起）
- `release.yml` 0-job、iOS、Android 构建失败（Windows 内测不受影响）。