# 2026-09-10

## 决策
- **内测仅面向 Windows**：`release.yml`(0-job) / iOS / Android 的 CI 失败均为预存在问题，**不在内测范畴，挂起**，不排障。

## 业务板块深化：审批意见留痕
- 新增 `approvals.setStatus(id, status, comment)`：通过可填意见（可留空）、**驳回必填理由**（否则拒绝，仍为待审批）；历史 `statusHistory` 条带 `comment` 留痕。
- `update()` 也支持携带 comment 写历史。
- `detailData` 历史回显 `comment`；详情时间线展示意见（`状态 · 时间 · “意见”`）。
- UI：通过/驳回按钮走 `prompt` 收集意见（驳回留空给 toast 提示）。
- 新增 `_approval_comment_test.js` 18 断言。
- 门禁：**110 套件 0 失败** + 四端同源（sync-clients 同步 approvals.js）。

## 发布 v1.0.19（Windows 内测）
- bump-version patch → 1.0.19，四端+SW v18+version.json 同步；`npm run client:sync` 补同步 webroot(index/sw/version.json 漂移)后同源 ✅。
- 构建：`npm run dist:local` → `绿角犀 Office Setup 1.0.19.exe`(75.3MB) + `绿角犀 Office 1.0.19.exe`(75.1MB portable)。
- 门禁：**110 套件 0 失败**。提交 `b0ea236`，tag `v1.0.19`，已 push（main 9569e74..b0ea236）。
- 创建 GitHub Release v1.0.19（id=385978086）；上传资产 Setup/Portable（ASCII 名 Office.*.1.0.19.exe）+ latest.yml。

## 工作台清理 + 文件关联增强（提交 e668dfd）
- **移除**工作台「业务概览」「成交额趋势」两张卡（index.html + shell.js renderBizTrend/renderBizOverview 及调用）。底层 orders.dashStats/aggregateDaily/trend 纯函数保留（模块导出 + 测试依赖）。
- **单实例锁**：main.js `app.whenReady` 内 `if (!app.requestSingleInstanceLock()) { app.quit(); return; }` —— 修"双击文件打开应用但内容不同步"：不再重复起实例，转交首实例 second-instance 载入。
- **TXT 关联**：package.json fileAssociations 增 `.txt`。
- **XMind 打开**：electron/file-args KNOWN_EXT 增 `.xmind`；fileAssociations 增 `.xmind`；前端 tryparseXmind（zip 内 content.json 现代 / content.xml XMind8 旧版 → 大纲 writer），OS.Importer 导出；shell mimeFor 增 xmind。新增 `_xmind_test` 12 断言，`_file_args_test` 补 .xmind。
- 门禁 **111 套件 0 失败** + 四端同源。

## CI 排查收口（桌面构建复绿）
- 根因修复：空证书注入崩溃 → `build-desktop`/`release.yml` 仅当提供签名 secret 时注入证书（`9569e74`）。
- 推送 `9569e74` 至 GitHub `main`（此前因 PAT 缺 `workflow` scope 被拒）。
- **Build Desktop (Electron) 验证 = success**（run 34425557655）。

## 凭证就绪（HTTPS 推送）
- 新 PAT 含 `repo`+`workflow`，存放：`C:\Users\Administrator\.lvjiaoxi-git-credentials`（仓库外）。
- **坑**：git HTTPS 推送须用 Basic/URL 内嵌 token（`Authorization: token` scheme 不被 git 接收→invalid credentials）。
- token 明文只在生成瞬间显示，及时抓取；生成页 scope 父子自动合并会弹提示，不影响 repo/wokflow 保留。

## Git remote
- remote 已从 SSH 切到 HTTPS `https://github.com/stvode-cyber/lvjiaoxi-office.git`。
- 上次会话的 SSH `Permission denied (publickey)` 已绕开；本地与远端 `main` 一致（`9569e74`，SYNCED）。

## 遗留（非内测范畴，挂起）
- `release.yml` 0-job、iOS、Android 构建失败（Windows 内测不受影响）。

## v1.0.20 升版 + 构建 + 本机验证（交接前收口）
- **版本晋升**：bump → 1.0.20，package.json + version.json + 四端同步一致（版本全落点统一）。
- **构建**：清理 dist 中过期的 1.0.19 产物后，`npx electron-builder --win --publish never` 生成 `绿角犀 Office Setup 1.0.20.exe`(79.0MB) + `绿角犀 Office 1.0.20.exe`(78.7MB) + latest.yml。**注意**：此前有一残留后台构建进程在打包 1.0.19（版本未晋升时启动），属过期产物，已忽略重建。
- **本机安装验证**（`C:\Users\Administrator\AppData\Local\Programs\lvjiaoxi-office`，FileVersion=1.0.20，.xmind/.txt 关联已注册）：经 CDP（--remote-debugging-port）实测抓取渲染界面确认——
  - 冷启动双击 .txt：标题《测试文档.txt · 绿角犀 Office》，正文完整渲染 ✅
  - 已运行再双击 .txt（second-instance）：内容同步 ✅
  - 双击 .xmind：标题《项目规划 · 绿角犀 Office》，主页/任务A/子项a/任务B 全部渲染，多标签顺序打开正常 ✅
  - 验证结论：修复前「只强出应用无内容」为旧版未装所致；v1.0.20 单实例锁+握手+队列兜底实际生效。
- **交接文档**：`.workbuddy/handoff-2026-09-10.md`（首次写入）。

## 接手待办（未完成，见交接文档 §6/§9）
- git add+commit+push main 至 GitHub（当前 ahead 1 commit + 版本升级/worktree 未提交）；打 tag v1.0.20。
- 创建 GitHub Release v1.0.20 预发布，上传 Setup/Portable/latest.yml 三件套。
- VPS 更新源上传（用户侧执行 `scripts/publish-latest.bat`，lujax.fun 在沙箱不可达）。