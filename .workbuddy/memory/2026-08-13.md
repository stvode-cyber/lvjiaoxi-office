# 2026-08-13

## 绿角犀 Office · UI 重构为 Office / Fluent 风格
- 目标：学 Microsoft 365 视觉语言（中性灰 chrome、品牌蓝强调、标签页功能区、分组底注、状态栏、文件后台）。
- 新增 `app/js/icons.js`（Fluent 线性 SVG 图标集，currentColor）。
- 新增 `app/js/ribbon.js`（`OS.Ribbon.create({file, tabs:[{id,label,groups:[{label,items}]}]})` → 自包含 `.ribbon-comp`，含 文件/开始/插入/设计/视图 标签 + 分组底注 + 按钮/分栏/下拉/颜色/选择）。
- 重写 `app/css/style.css` 为 Fluent 设计令牌（浅/深双主题、4px 圆角、轻阴影、标题栏/功能区/状态栏/Backstage）。
- 重构 `app/index.html`：标题栏(品牌+快速访问保存/撤销/重做+文档标题+兼容徽章) + 功能区区 + 状态栏 + 文件后台容器。
- 重构 `app/js/shell.js`：注入顶栏图标、挂载 ribbon、渲染状态栏(缩放/兼容)、文件后台(信息/保存/导出/打印/关闭)、快速访问按钮。
- 五大模块(writer/spreadsheet/presentation/mindmap/pdf)接入新 ribbon，返回 `ribbon` 组件（旧 `ctx.ribbonHost.appendChild` 改为组件化）；保留全部命令逻辑与导出/导入。
- 模块契约：mount 返回 `{serialize, exportAs, destroy, ribbon, ...}`，shell 把 `instance.ribbon.el` 注入 `#ribbon-host`；文件标签回调 `ctx.openBackstage`。
- 验证：10 个 JS 全部 `node --check` 通过；jsdom 冒烟测试确认 5 模块均渲染 ribbon-comp（writer 4标签/其余 4标签/pdf 2标签）且 backstage 打开正常。
- 预览服务：`python -m http.server 8082`（后台任务 G77n5q）。

## 绿角犀 Office · 增加「任务进程」(Task Progress) 系统
- 新增 `app/js/tasks.js`：`OS.Tasks.run(title, fn(reporter), opts)`，全局异步任务管理器，仿 Office 保存/上传状态。
  - reporter：`step(label,p)` / `progress(p)` / `setCancelable(c,fn)` / `cancel()`；task 含 `status(running/done/error/canceled/queued)` 与 `progress(0..1 或 null=indeterminate)`、`cancelable`。
  - UI：状态栏「任务」按钮(带活动数徽章) + 右侧滑出面板 `#task-panel`（任务卡：标题/状态/步骤/进度条/取消）。`opts.quiet` 不弹面板不 toast（用于自动保存）。`Alt+T` 开关面板。
- 接入点：shell `saveNow()`（显式保存走任务，自动保存走静默 `persist()`，避免刷屏）；`onFile` 导入整体包成任务并把 `OS.Importer.importFile(f, onProgress)` 的进度透传；导出菜单统一经 `runExport()`（原生格式走 `OS.Exporter.exportDoc(doc,fmt,onProgress)`，其余走模块 `exportAs`）。
- `import-ooxml.js`：`importFile(file, onProgress)` 增加进度回调（读取/解压/解析阶段）。`export-ooxml.js`：`exportDoc(doc, fmt, onProgress)` 在 `zip.generateAsync({onUpdate})` 透传真实百分比。
- `icons.js` 新增 `tasks`(剪贴板勾选)、`spinner` 图标；`index.html` 在 shell 前加载 `tasks.js`；`style.css` 追加任务面板/卡片/进度条/indeterminate 动画/状态栏任务按钮（双主题）。
- 验证：全部相关 JS `node --check` 通过；`OS.Tasks` 单元 jsdom 测试 8/8（面板显隐、进度条 100%、完成态）；全量集成 jsdom 测试 7/7（加载全部脚本→新建 writer→saveNow 生成任务卡并自动完成→面板开关正常）。
- 预览：http://127.0.0.1:8082/app/index.html

## 绿角犀 Office · 开始页（模板画廊 + 最近）
- 首页从 hero+模块能力卡 升级为 Office 风格「开始页」：顶部 hero(品牌渐变+打开文件) + 左右两栏(新建模板画廊 | 最近文档)。
- 新增 `app/js/templates.js`：`OS.Templates = { list, byId(id), groups }`。每个模板 `{id, module, name, desc, accent, thumb(SVG), build()->合法模块 data}`。
  - 分组：writer(空白/工作报告/会议纪要/简历)、spreadsheet(空白/月度预算/项目计划/成绩表)、presentation(空白/商务汇报/教学课件)、mindmap(空白/项目计划/读书笔记) —— 共 14 个。
  - thumb 用统一 Fluent 占位 SVG（doc/sheet/pres/mind 四型，accent 着色）。
- `shell.js`：`newDoc(type, templateId)` 支持模板新建（store.create 后覆盖 data+name+compat 再 put）；`renderDashboard()` 渲染 `#tpl-gallery`（按模块分组卡片，含 SVG 缩略图）与 `#recent-grid`；`docCard` 升级为大类型图标色块卡；点击委托新增 `[data-tpl]` → `newDoc(module, tpl)`。
- `index.html`：dashboard 改 `.start-page` 结构(hero+start-layout[tpl-gallery|recent-grid])，在 shell 前加载 `templates.js`，移除旧 module-cards。
- `style.css`：替换旧 hero/card 样式为 start-hero / start-layout / tpl-group / tpl-row / tpl-card(hover 抬升+蓝边) / 升级 doc-card(42px 图标 chip，accent 用 color-mix 着色)；含 760px 移动端适配。
- 验证：templates.js/shell.js `node --check` 通过；开始页 jsdom 测试 9/9（模板数14、从模板新建生成正确 data 含公式/脑图节点边、画廊卡片数=模板数）。
- 预览：http://127.0.0.1:8082/app/index.html

## 绿角犀 Office · AI 助手（Copilot 风格，本地优先）
- 新增 `app/js/ai.js`：`OS.AI` 框架。本地规则引擎 `OS.AI.local` 纯函数：summarize(抽取式摘要/词频+位置权重)、polish(冗余删除+口语转书面+标点规范)、rewrite(同义词+句式微调)、expand(关键词扩写)、continueWriting(续写)、outline(大纲)、translate(诚实版术语对照+云端预留说明)、explain(概念表命中/关键词提示)。`OS.AI.runLocal(mode,text)` 路由；`OS.AI.run({mode,system,user,onDelta,signal,allowCloud})` 统一入口（有 provider 且未开数据不出域→OpenAI 风格 SSE 流式，否则本地）。`OS.AI.setProvider({endpoint,apiKey,model})` 配置云端；`OS.AI.available()` 返回 {local,cloud}。
- 外壳集成 `app/js/shell.js`：标题栏 `#btn-ai`(spark 图标) + 右侧 Copilot 抽屉 `#ai-drawer`(上下文条+指令卡+对话流+输入区)。快捷键 `Ctrl/Cmd+Shift+A` 开；命令面板加「打开 AI 助手」；Esc/关闭按钮关。`aiContext()` 通用文档上下文：`selText()`(window.getSelection)、`fullText()`(按 doc.type 从 serialize/data 提取：writer 去标签、spreadsheet 拼 cells、presentation 拼 elements、mindmap 拼 nodes、pdf 空)、`replaceSelection/insertText`(contenteditable 用 execCommand('insertText')，否则复制到剪贴板+toast)。`runAI(mode,instruction)` 组装 user(选中优先，否则全文，否则自由输入)→`OS.AI.run`→`typewriter` 逐字显示→完成后「插入到文档」按钮(translate/explain 不显示)。
- `index.html`：tb-right 加 `#btn-ai`；body 加 `#ai-drawer`(ai-head/ai-ctx/ai-cards/ai-chat/ai-input)；shell 前加载 `ai.js`。
- `icons.js` 加 `spark` 图标；`style.css` 加 `.ai-drawer`(fixed 右侧360px/aiSlideIn 动画)、`.ai-ctx`、`.ai-cards`/`.ai-card`(33% 弹性网格)、`.ai-chat`/`.ai-msg`/`.ai-bubble`(用户蓝底/AI 描边)、`.ai-actions`、`.ai-input`，含 760px 全宽适配（双主题）。
- 设计原则：本地优先零网络、数据不出域开关强制本地、云端为增强项（PRD 3.6）。
- 验证：ai.js/shell.js/icons.js `node --check` 通过；jsdom 集成测试 16 项（本地引擎 8 函数+runLocal+available 10 项；抽屉开关/卡片数9/对话气泡/AI 回复非空/关闭 6 项）。
- 注：jsdom outside-only 不自动触发 DOMContentLoaded，boot 绑定需手动 dispatch（真实浏览器正常）；测试已覆盖。
- 预览：http://127.0.0.1:8084/app/index.html（原 8082 进程已失效，重启 8084）

## 绿角犀 Office · Writer 审阅 / 批注评论系统
- Writer 新增「审阅」ribbon 标签（批注组：新建批注/上一条/下一条/全部解决；视图组：显示隐藏评窗）。`writer.js` 重写：wrap 加 `.writer-wrap`(row 分栏) + 右侧 `#cmt-panel` 评论侧栏。
- 批注数据模型：`doc.data.comments = [{id, quote, author, createdAt, resolved, replies:[{author,text,createdAt}]}]`；文中锚点用 `<span class="cmt" data-cid>`，`serialize()` 返回 `{html, comments}`（与 store 自动保存打通）。
- 交互：选中文字→「新建批注」（range.surroundContents，跨节点降级 extractContents+insertNode）；侧栏卡片含头像/作者/时间/引用/回复列表/回复输入/解决·删除；点卡片跳正文高亮(flash)，点文中 span 联动高亮卡片；解决态双向同步(卡片+文中 span)；删除保留文字去掉高亮；全部解决/上一条下一条(待处理导航)。`#cmt-toggle` 显隐面板（隐藏时文中高亮弱化）。打开文档时 `syncResolvedSpans()` 还原已解决态。
- `icons.js` 新增 `comment`/`check`/`check-all`/`trash`/`arrow-up`/`arrow-down`（复用 `eye`/`chevron-right`）。`style.css` 追加 `.writer-wrap` row、`.cmt-panel`/`.cmt-card`/`.cmt-quote`/`.cmt-reply`/`.cmt-add`/`.cmt-actions` 及文中 `span.cmt` 黄色高亮 + `.cmt-resolved`(灰虚线) + `.cmt-flash`(蓝→黄动画) + `.cmt-hidden` 弱化 + 760px 面板收窄（双主题）。
- 验证：writer.js/icons.js `node --check` 通过；jsdom 集成测试 12/12（面板挂载、审阅 tab、空态、新建锚点、卡片、回复、解决、文中同步、面板显隐、重新打开还原已解决态）。
- 防御：所有 `scrollIntoView` 调用加存在性判断（jsdom 无该方法）。
- 预览：http://127.0.0.1:8086/app/index.html（重启）

## 绿角犀 Office · 批注 DOCX 原生往返（导出+导入闭环）
- 补全 Writer 批注的「保存出去再读回来」：此前 `buildDocx` 完全丢弃 `doc.data.comments`，审阅工作一导出就没了；现在走原生 WordprocessingML 批注部件，与 `OS.Exporter`/`OS.Importer` 的往返闭环一致。
- 导出 `export-ooxml.js` `buildDocx`：读 `comments`，生成 `word/comments.xml`（主评论 `<w:comment w:id>` + 回复 `<w:comment w:parentId>`，已解决带 `w:done="1"`）；document.xml 对 `<span class="cmt" data-cid>` 包裹 `w:commentRangeStart`/`w:commentRangeEnd` + `w:commentReference` 锚点；`[Content_Types].xml` 加 comments 覆盖、`word/_rels/document.xml.rels` 加 comments 关系、`styles.xml` 加 CommentText/CommentReference 样式。全部 `hasComments` 守卫，无批注时不产生任何多余部件。cid→w:id 用含回复偏移的运行计数器，保证 span 与 comments.xml 的 id 对齐。
- 导入 `import-ooxml.js` `parseDocx`：新增 `parseComments()` 解析 `word/comments.xml`（按 parentId 聚合成 replies）；`runsToHtml`/`blockFromP` 处理 `commentRangeStart/End/Reference`（引用锚点跳过，范围包成 `<span class="cmt" data-cid>`，并从正文跨度取真实 quote 兜底）；跨段批注在段末降级闭合。返回 `data.comments`（id 用 w:id 字符串、resolved 由 done 映射）。
- 验证：jsdom 联调 19/19（导出 10 项 + 导入还原 9 项含回复/已解决/span 还原 + 无批注不产生 comments 部件 2 项）；临时测试脚本跑完即删。
- 预览：http://127.0.0.1:8090/index.html（旧 8086 进程疑似失效，重启 8090；服务器根目录为 app/，故路径不含 /app 前缀）

## 绿角犀 Office · 开始页增强：模板搜索 + 收藏
- 延续开始页（模板画廊）做两项增强：`templates.js` 新增 `OS.Favorites`（模板收藏，UI 偏好，落 `localStorage` key `lvjiaoxi-favorites`，localStorage 不可用时回退内存 Set；API `all/has/isFav/toggle(id)/set(id,on)`）；`shell.js` 把 `renderDashboard` 拆出 `renderGallery(query)`（实时搜索：按 名称/描述/模块中文名 过滤，命中仅显示「搜索结果」分组，无结果显示空提示；无搜索词时顶部「★ 收藏」分组置顶、模块分组隐藏已收藏项避免重复）。每张模板卡右上角加 `.tpl-star` 星标按钮切换收藏，`e.stopPropagation()` 防止误触发新建；搜索框 `#tpl-search` 在 boot 绑定 `input` 事件实时重渲染。
- `index.html`：start-templates 区加 `<input id="tpl-search">`；`style.css`：`.tpl-search`(Fluent 输入框+accent 聚焦环)、`.tpl-card{position:relative}`、`.tpl-star`(绝对定位圆钮，.on 金橙填充)、`.tpl-card.is-fav` 金橙描边、`.tpl-group.fav .tpl-group-head.fav` 强调色、`.tpl-empty`（双主题）。
- 验证：templates.js/shell.js `node --check` 通过；jsdom 集成测试 19/19（基础、收藏持久化到 localStorage、收藏分组渲染与去重、搜索过滤与类型名命中、无结果空提示）；临时测试脚本跑完即删。测试发现并修正：jsdom 下 `window.eval` 全局与 window 不一致导致 bare localStorage 解析失败，改用真实 `<script>` 元素注入+`runScripts:"dangerously"` 才正确（真实浏览器无此问题）。
- 预览：http://127.0.0.1:8090/index.html（服务器根目录为 app/，路径不含 /app 前缀）

## 绿角犀 Office · Writer 选区浮层 + AI 一键操作（可逆）+ AI 审阅建议
- 延续 AI 助手与批注系统：给 Writer 增加**选中文字即浮出**的迷你工具栏（不进 ribbon，贴近选区）。`writer.js` 新增：选区非空且在 `.writer-page` 内时（document `selectionchange` 事件）浮层 `#sel-toolbar` 浮于选区上方（上方空间不足自动放下方）；按钮：润色/改写（**替换选区**）、扩写/解释（**选区后插入**）、建议批注（AI 生成审阅建议并作为批注首条回复）、批注（手动）。
- 可逆性：AI 文本改动全部走 `document.execCommand`（delete+insertText 替换；collapseToEnd+insertParagraph+insertText 插入），因此与 Writer 既有 `exec(undo/redo)` 及全局 Ctrl+Z 共用**原生撤销栈**，一键可撤回。
- 防丢选区：用 `selectionchange` 时克隆并缓存 `Range`（`storedRange`），按钮 `mousedown` 阻止默认以避免点击失焦清空选区；点击后从缓存 Range 还原选区再 exec。
- 重构批注逻辑：`addComment` 拆出 `getSelectionRange`/`wrapSelection`/`finalizeNewComment`/`addCommentWithSuggestion`（AI 建议以 `replies:[{author:"AI 建议",...}]` 写入，不破坏原文）；`destroy` 清理全局 `selectionchange`/`resize` 监听。
- `ai.js` 本地引擎新增 `local.critique(text)`：基于句数/口语词/结论缺失/标点/关键词密度产出**诚实的本地审阅建议**（非整句改写），用于「建议批注」。
- `style.css`：`.sel-toolbar`（absolute/z-index:60/Fluent 圆角阴影/不遮挡）+ `.sel-btn`（hover 蓝）。
- 健壮性：`positionSelBar` 对 `Range.getBoundingClientRect` 缺失/抛错做降级（默认 8/8 定位），避免无头环境崩溃（真实浏览器有该 API）。
- 验证：ai.js/writer.js `node --check` 通过；jsdom 集成 13/13（critique 产出含标题与具体建议；浮层选区显示；建议批注生成「AI 建议」回复且文中 span.cmt 锚点、操作后浮层隐藏；润色点击不抛错；选区清空浮层隐藏）；临时测试脚本跑完即删。
- 预览：http://127.0.0.1:8093/index.html（8090/8092 旧进程回收，重启 8093；根目录为 app/，路径不含 /app 前缀）

## 绿角犀 Office · 用户自定义模板（保存为模板 + 开始页「自定义」分组 + 管理/删除）
- 延续模板画廊与收藏：让用户把当前文档存成可复用模板。`app/js/custom-templates.js` 新增 `OS.CustomTemplates`（UI 偏好，落 `localStorage` key `lvjiaoxi-custom-templates`，localStorage 不可用时回退内存数组）：`all()`/`byId(id)`(返回带 `build()` 的模板形态，数据 `JSON` 深拷贝)/`add({name,module,data})`(自动取模块空白模板的 accent/thumb)/`remove(id)`。
- `templates.js` `byId` 现在先查内置再查 `OS.CustomTemplates`，使 `newDoc(module, customId)` 走统一 `build()` 路径（新建时深拷贝，避免引用共享）。
- `shell.js` `renderGallery`：搜索时 `T.list.concat(customs)` 一起过滤（自定义模板也能被「自定义」关键词命中）；无搜索词时新增「自定义」分组（已收藏项仍隐藏），自定义卡带 `.tpl-del` 删除按钮（`confirm` 后 `remove` 并重渲染）；`makeCard` 增加 `is-custom` 卡型与删除按钮。`openBackstage` 新增「我的模板」导航：输入名称 + 「保存为模板」(取 `t.instance.serialize()` 当前文档数据) + 列表/删除已有模板（局部 `renderCustomList`）。
- `index.html`：templates.js 之后加载 `custom-templates.js`。`style.css`：`.tpl-card.is-custom`(虚线边框) + `.tpl-del`(绝对定位圆钮 hover 红) + `.bs-tpl-form`/`.bs-input`/`.bs-tpl-list`/`.bs-tpl-item`/`.bs-tpl-name`/`.bs-tpl-mod`/`.bs-tpl-del`（双主题）。
- 验证：3 文件 `node --check` 通过；jsdom 集成 16/16（新增持久化到 localStorage、byId 解析自定义模板并 build 还原、画廊「自定义」分组与 2 张自定义卡带删除钮、搜索命中自定义/内置/无结果空提示、删除后长度与画廊同步）；临时测试脚本跑完即删。
- 预览：http://127.0.0.1:8098/index.html（8090-8097 旧进程陆续回收，重启 8098；根目录为 app/，路径不含 /app 前缀。注：本环境跨轮后台进程会被回收，若 404 让我重启即可）

## 绿角犀 Office · 自定义模板分享包（导出/导入 .json）
- 延续自定义模板：让用户把模板**带走/分享**。`custom-templates.js` 新增 `exportAll()`（导出标准分享包 `{"format":"lvjiaoxi-templates","version":1,"templates":[...]}`，字段 id/name/desc/module/accent/thumb/data/createdAt）与 `importFrom(text)`（解析+校验：非法 JSON、非模板结构、缺 data、未知模块均计 errors；按 id 去重，已存在跳过 skipped，新增 added；缺 accent/thumb 自动取模块空白模板默认值；返回 `{added,skipped,errors}`）。
- `icons.js` 补 `upload` 线性图标（与 `download` 对称）。
- `shell.js` `openBackstage`「我的模板」面板新增 `.bs-tpl-actions` 行：`导出模板包`（OS.util.download 落 `绿角犀模板包.json`）+ `导入模板包`（隐藏 `#bs-tpl-file` 文件输入 → `f.text()` → `importFrom` → toast 汇总 + 刷新列表与开始页）。
- `style.css`：`.bs-tpl-actions`（flex + wrap）+ `.btn` 内联图标对齐。
- 验证：3 文件 `node --check` 通过；jsdom 集成 20/20（导出结构含格式标记与 2 模板、导入往返恢复、重复导入跳过、非法 JSON/非模板结构/缺 data/未知模块均拒、新包追加）。临时测试脚本跑完即删。

## 绿角犀 Office · 四项功能迭代（AI 浮层推广 / 批注附录 / AI 流式+云端 / 视觉打磨）
- **① AI 选区浮层推广到全模块**：新增共享工厂 `app/js/ai-selbar.js`（`OS.AI.createSelToolbar({container, features, getSelection, onApplied})` → `.sel-toolbar` 浮层，position:fixed 按选区视口坐标定位；按钮 润色/改写(替换)/扩写/解释(插入)/建议批注/批注/助手；selectionchange+scroll+resize 监听、refresh/destroy）。各模块通过 `getSelection()` 提供 `{text, rect, replace, insertAfter}` 接入：
  - writer：复用工厂（原内联浮层删除），保留 建议批注/批注 按钮（`addCommentWithSuggestion` 缺省时 `OS.AI.local.critique`）。
  - spreadsheet：仅 替换+助手（单元格 contenteditable，选区在 `td[contenteditable]` 内）。
  - presentation：替换+插入+助手（`.el[contenteditable]` 文本元素）。
  - mindmap：替换+插入+助手（编辑器为 `document.body` 上的 `textarea.mm-editor`，用 selectionStart/End 读选区，`commitEditor()` 回写节点）。
  - 关键顺序坑：`ai.js` 末尾 `OS.AI = {...}` 整体重赋值，会抹掉早加载的 `createSelToolbar`，故 `ai-selbar.js` 必须在 `ai.js` 之后、`shell.js` 之前加载（已调整 index.html 脚本序）。
- **② 批注导出进附录**：writer `exportAs('html'/'docx')` 末尾附加「批注附录」区块（列出引用原文/作者/时间/已解决态/逐条回复）；`export-ooxml.js` `buildDocx` 在正文 body 末尾追加「批注附录」标题与逐条 `<w:p>`（仅 `hasComments` 时），原生 `comments.xml` 不变。新增 `commentsAppendixHtml()`（writer）与 `buildCommentsAppendix()`（export-ooxml），`esc()` 已存在复用。
- **③ AI 真正流式 + 云端配置**：`ai.js` `run()` 本地分支改为**模拟流式**（按标点/换行切 `streamChunks` + 12ms `sleep` 逐步 `onDelta`），返回结构改为 `{text, source}`（source=local|cloud）便于 UI 标注来源；云端 `completeCloud` SSE 不变。`shell.js` `runAI` 实时把 `onDelta` 写入聊天气泡（打字机效果）+ 显示「☁ 云端 / 🖥 本地」状态 + 完成后「插入到文档/替换选区」按钮（新增 `addAIActions`）；`openAccount` 新增 AI 云端配置区（endpoint/apiKey/model + 测试连接/保存/清除，`setProvider` 持久化到 `OS.settings.aiProvider`，boot 时还原）；`OS.AI.available()` 反映 cloud 状态。
- **④ A 级像素保真（Office 视觉打磨）**：titlebar 加轻阴影；ribbon 激活标签 `color:var(--accent)` + 3px 下划线；分组标题 `uppercase`+字母间距（Office 底注风格）。已有模板卡 hover 抬升/阴影/蓝边、group 竖向分隔已具备，保留。
- 验证：`node --check` 8 文件全通过；新建 `app/_feat_test.js`（jsdom 集成，注入 canvas 2d shim + JSZip）**29/29**（AI 审阅、浮层工厂、四模块浮层创建与挂载不抛错、AI 流式返回结构与 onDelta 调用、云端 setProvider/getProvider/available、Writer HTML 批注附录、DOCX 批注附录含/不含+无 comments.xml）；旧 `app/_sel_test.js` Writer 回归 **13/13**。预览 http://127.0.0.1:8099/index.html（根目录 app/）。
- 测试环境坑（重要，复用）：本机 `node.exe` 是原生 Windows，require 第三方包必须用 **Windows 盘符绝对路径**（`C:/Users/.../node_modules/jsdom`），不能用 POSIX `/c/Users/...`（会被解析成 `<cwd>:\c\Users` 而找不到）；jsdom 的 canvas getContext 会打印 "Not implemented" 且返回 null，需自行给 `document.createElement('canvas')` 的实例覆盖 `getContext` 提供 measureText。

## 绿角犀 Office · Writer 查找/替换 + 字数统计
- 新增 Writer **查找/替换**浮层与底部**状态栏**。DOM 重构：`.writer-wrap` 改为 column，内部包 `.writer-main`(row: doc-surface + cmt-panel)，底部新增 `.writer-statusbar` 与绝对定位 `.find-panel`（浮于右上角）。
- 查找：`createTreeWalker` 遍历 page 文本节点收集匹配，按 **从后往前 splitText** 包裹 `<mark class="find-hit">`（保证同文本节点内偏移有效，避免 Range.surroundContents 异常），`querySelectorAll('.find-hit')` 取文档序；`.find-cur` 高亮当前并 `selectNodeContents`+`scrollIntoView`（均 try/catch 降级，jsdom 无 scrollIntoView）。支持 上一个/下一个（环绕）、区分大小写。
- 替换：当前匹配 `textContent=replacement` 后 `unwrap`（replaceChild 文本节点 + `parent.normalize()` 合并相邻文本，恢复原始结构供批注/导出），`replaceAll` 批量；替换后不重新匹配 replacement（防死循环）。count 归零清标记。
- 字数统计：`updateStatus()` 取 `page.textContent` 去空白得字数、含空白字符数、`p/h1-4/li/blockquote/td/pre` 段落数、约 (字数/300) 分钟；挂载时与每次 `input` 后刷新状态栏。
- ribbon「编辑」组加 `查找 / 替换 (Ctrl+F)` 按钮（icon search）；`page` keydown 加 `Ctrl/Cmd+F` 开浮层；`destroy` 调 `clearFind()`。模块实例暴露 `findAPI`（`open/close/run/next/prev/replace/replaceAll/setQuery/count/status/words`）便于测试与将来指令集成。
- `css/style.css`：`.writer-main`、`.writer-statusbar`、`.find-panel/.find-row/.find-input/.find-btn(.primary)/.find-count/.find-case`、`.find-hit`/`.find-cur` 高亮（黄/橙，与批注黄底区分）。`.writer-wrap` 由 row 改 column。
- 验证：`node --check` 通过；新建 `app/_find_test.js`（jsdom）**23/23**（挂载+状态栏初始化、字数一致、查找命中数、导航环绕、替换当前 1 处、全部替换清标记且序列化无 find-hit 残留、区分大小写、关闭清理）；既有 `_feat_test.js` 29/29、`_sel_test.js` 13/13 无回归。预览 http://127.0.0.1:8101/index.html（根目录 app/）。

## 绿角犀 Office · 表格多选区 + 状态栏聚合 + 填充（相对引用）
- 延续 Writer 查找/替换后，补全表格高频能力。原 `stSum` 状态栏元素**从未填充**（死代码），且无多单元格选区——只有单格 `selected`。
- 新增多单元格矩形选区：`anchor`（起点）+ `selected`（活动格），支持 **拖拽选择**（mousedown/mousemove/mouseup，dragMoved 防 click 重置）+ **Shift 单击/方向键扩展**（Shift+Arrow 不提交仅扩展）；`paint()` 给活动格加 `.selected`、范围内加 `.in-range`；`selectCell` 重置 anchor。`updateStatus()` 现基于整个选区实时算 **求和/计数/平均值**（仅数值，文本格不计入），激活了死代码 `stSum/stSel`。
- 新增**填充**：`fillSelection()` 将 `anchor` 内容按相对引用填充到整选区；公式走 `FE.adjustFormula(base, dRow, dCol)`（新增于公式引擎，正则定位引用、`跳过字符串字面量/函数名`、`$` 绝对引用列前/行前均尊重，返回 = 开头新公式），纯值/文本按值复制。公式「填充」按钮加在 ribbon「公式」标签下「填充」组（icon `fill`，新增于 icons.js）。
- `icons.js` 补 `fill` 线性图标；`style.css` 加 `table.sheet td.in-range` 高亮。模块实例暴露 `selectCell/extendTo/fillSelection/status/adjustFormula` 便于测试。
- 关键 bug 与修复：① `updateStatus` 误用了 `paint` 作用域内的 `sC/sRR` 变量 → 在 `updateStatus` 内基于 `selected` 重新解析；② `adjustFormula` 正则 `\d+` 未捕获为分组导致 `m[3]` 为 `undefined` → 改为 `(\d+)`。
- 验证：`node --check` 通过；新建 `app/_sheet_test.js`（jsdom）**18/18**（状态栏聚合 求和30/计数3/平均10/含文本不计入/选区高亮类；adjustFormula 6 例含 $ 绝对与字符串内 a1 不误改；填充公式相对偏移 A2=A B2+1/A3=B3+1 且计算值 21/31；数值复制填充）。既有 `_feat_test.js` 29/29、`_sel_test.js` 13/13 无回归。预览 http://127.0.0.1:8103/index.html（根目录 app/）。

## 绿角犀 Office · 表格图表（选区数据 -> SVG 柱状/折线/饼图）
- 延续表格多选区/聚合/填充：新增**图表**能力（纯 SVG，无外部库，随数据实时重绘）。`spreadsheet.js` 新增：
  - `extractChartData(rg)`：从选区矩形提取 `categories` 与 `series`。自动识别**首列文本作分类**（`colLooksText`）与**首行文本作表头**（`series` 名称取表头、数据从 r1+1 起）；纯数值矩形则分类=行号、每列一系列。辅助 `cellNum/cellText/isNumCell/isTextCell`。
  - `barSVG/lineSVG/pieSVG`：固定几何（560×320，左侧 y 轴 4 刻度 `niceMax`、底部分类轴），`legendSVG` 图例；柱支持正负基线 `yOf(0)`；折线每系列 polyline+圆点；饼取第一系列正值做扇区 `arc()` + 百分比图例。`CHART_COLORS` 调色板。
  - `renderCharts()`：渲染到 `.sheet-charts` 容器（每个 `.chart-card` 带标题+删除×），在 `recompute()` 末尾调用 → **编辑单元格即实时刷新图表**。`addChart(type)` 把当前 `rangeRect()` 存为 `data.charts:[{id,type,range,title}]` 并 `markDirty`，无数值选区 toast 警告不生成。
  - ribbon「插入」标签「图表」组：柱状图/折线图/饼图（icons.js 补 `bar-chart/line-chart/pie-chart`）。模块实例暴露 `addChart/renderCharts/charts/extractChartData/chartSVG`。`blank()` 加 `charts:[]`。
- `css/style.css`：`.sheet-charts`（横向滚动条带）、`.chart-card`（白卡轻阴影圆角）、`.chart-head/.chart-title/.chart-close(hover 红)/.chart-svg/.chart-svg-el`（双主题）。
- 验证：`node --check` 通过；新建 `app/_chart_test.js`（jsdom）**20/20**（extractChartData 表头/首列识别与纯数值矩形、柱状图 6 柱借助 class=chart-bar 计数、折线 2 条 path、饼 3 扇区+百分比、真实单元格编辑改值后 SVG 变化（commit→recompute→renderCharts 清缓存）、删除图表、纯文本选区不生成）。既有 `_feat_test.js` 29/29、`_sel_test.js` 13/13、`_sheet_test.js` 18/18 无回归。预览 http://127.0.0.1:8105/index.html（根目录 app/）。

## 绿角犀 Office · 演示演讲者备注（编辑 + 放映显示 + PPTX 双向）
- 新增 **演讲者备注 / 讲稿** 能力（PRD 3.3 演讲者视图延伸）。`presentation.js`：`blank()` 每页加 `notes:""`；侧栏加「演讲者备注 / 讲稿」textarea（`.notes-input`）；`syncPanel` 回填、`input` 写入 `data.slides[cur].notes` 并 `renderThumbs()` 同步；放映视图 `showPresenter` 右侧加 `.p-notes-pane`（`.p-notes` 展示当前页备注，空则「（本页暂无备注）」italic）；模块实例暴露 `setCur/setNotes/notes` 便于测试。`css`：`.notes-input`、`.presenter .p-notes-pane/.p-notes-head/.p-notes(.empty)`（双主题），`.p-main .slide` 高度略降给备注留位。
- **PPTX 双向**：`export-ooxml.js` `buildPptx` 仅当 `slides.some(s=>s.notes.trim())` 时产出 `ppt/notesSlides/notesSlideN.xml`+rels（仅含备注的页）、`ppt/notesMasters/notesMaster1.xml`+rels（复用 theme1）、`[Content_Types].xml` 追加 notesMaster+notesSlide override、`ppt/presentation.xml` 加 `notesMasterIdLst`、`presentation.xml.rels` 加 notesMaster 关系、`ppt/slides/slideN.xml.rels` 加 notesSlide 关系（rId2）。`import-ooxml.js` `parsePptx` 通过 `ppt/slides/_rels/slideN.xml.rels` 找 `/notesSlide` 关系 → 解析目标 → 提 `<a:t>` 文本（反实体化）回填 `slide.notes`。无备注时不产生任何 notes 部件。
- 验证：`node --check` 3 文件通过；新建 `app/_notes_test.js`（jsdom，注入 canvas 2d shim + JSZip）**13/13**（编辑/序列化 5、PPTX 导出按需产出 notesSlide1/notesMaster 且 notesSlide2 不产生/文本含换行/无备注零 notes 部件 5、导入往返第1页保留第2页为空 3）；修复测试 `ok()` 参数顺序（原 (name,cond) 调用却传 (cond,name) 会掩盖失败，改为 (cond,name)）。既有 `_feat/29`、`_sel/13`、`_find/23`、`_sheet/18`、`_chart/20` 全部无回归。预览 http://127.0.0.1:8107/index.html（根目录 app/）。

## 绿角犀 Office · 表格条件格式（高亮/色阶/数据条/前10/重复值）
- 新增 **条件格式** 能力（PRD 3.2 数据可视化延伸）。`spreadsheet.js`：
  - 数据模型 `data.condFormats:[{id,range,type,...}]`；5 类规则：`cell`（突出显示：gt/lt/eq/between/contains，含填充色）、`scale`（双色阶，按 min/max 线性插值 `lerpColor`）、`bar`（数据条，按 min/max 比例生成 `linear-gradient` 背景图 + `.cf-bar` 类）、`top`（项目选取：前/后 n 项或百分比）、`dupes`（重复值计数 >1 高亮）。
  - 单一数据源 `computeCF()` 返回 `ref -> {bg,color,bar}`（**cell/dupes/top 的 bg 经 `lerpColor(c,c,0)` 归一为 `rgb(r, g, b)` 空格格式，与 jsdom DOM 规范化一致**）；`applyCondFormat()` 在 `recompute()` 末尾调用，先按 `cfAppliedRefs` 复位旧样式并**恢复手动样式**，再按「手动样式优先」叠加条件格式（手动 bg 存在时条件 bg 不覆盖）。
  - ribbon「开始」标签新增「条件格式」组 + `split` 下拉按钮（`openRuleModal` 五种规则弹窗 + 分隔线 + 「管理规则…」`openManager` 列表删除/关闭）；`icons.js` 补 `format-cond`。弹窗含范围输入 + 类型字段 + 颜色取色器 + 6 色预设色板（`CF_SWATCHES`）。
  - **HTML 导出**（`exportAs("html")`）现把条件格式填充/数据条写入 `<td style>`（数据条用相对定位内层 div 模拟），与屏显一致。
  - 模块实例暴露 `commit/addCondFormat/removeCondFormat/condFormats/applyCondFormat/computeCF/cfStyleOf/openRuleModal/openManager`。`blank()` 加 `condFormats:[]`。
- 坑：① `let cfAppliedRefs` 放 `applyCondFormat` 声明之后，但 `mount` 初次的 `recompute()` 已调用它 → TDZ 报错；改在 mount 顶部 `var cfAppliedRefs=[]` 前置声明。② 数据条初版强制含 0 基线，与 Excel 默认（min→max）不符且测试期望 min 值 pct=0%，改为不加 0。③ `computeCF` 原存裸 hex，DOM 经 jsdom 规范为 `rgb(r, g, b)` 导致 `cfStyleOf` 比对失败，统一在 `lerpColor` 输出加空格。
- 验证：`node --check` 通过；新建 `app/_cond_test.js`（jsdom，OS 桩 + canvas shim）**39/39**（cell gt/between、scale 端点与插值、bar pct(最小0%/最大100%)、top 前2/后1、dupes、移除清零、手动样式优先、HTML 导出、openRuleModal/openManager 弹窗构建）。预览 http://127.0.0.1:8111/index.html（根目录 app/）。
- 继续工作（第6轮·Writer 页面设置）：`writer.js` 新增数据模型 `data.page{size,orientation,margin}`，`PAGE_SIZES`(A3/A4/A5/B5/Letter/16K)、`mmToPx`、`pageMetrics`、`applyPageSetup`（按 96dpi 计算纸张内容区 px，设 width/minHeight/padding 实现屏显纸张效果）、`ensurePrintStyle`（注入持久 `#pg-print-style` 含 `@page` 尺寸/页边距 + `@media print` 隐藏 ribbon/侧栏）、`openPageSetup` 弹窗、Ctrl+P 经原生打印。`icons.js` 补 `page-setup`；`style.css` 补 `.pg-*` 弹窗样式。序列化 persistence + `pageAPI` 测试钩子。坑：jsdom 把 `#fff` 序列化为 `rgb(255, 255, 255)`、`padding` 简写规范为单值 → 测试改读 longhand。新建 `_page_test.js` **28/28** 通过。

## 绿角犀 Office · 按序完成四项（备注讲稿 / 图表导出 / 切换动画 / 目录分页符）
按顺序执行既定四方向（用户「按顺序做」）：
- **① 备注/批注导出讲稿附录（A）**：`presentation.js` 新增 `speakerScriptHtml(doc)`（每页列标题+备注，顶层 h2/p 以便 DOCX 解析），经 `OS.speakerScriptHtml` 暴露；`exportAs('html')` 落 `<标题>_讲稿.html`，DOCX 分支在 `export-ooxml.js` 把 `buildDocx({type:"writer",data:{html:speakerScriptHtml(doc)}})` 当讲稿导出；shell 演示导出菜单加 `HTML 讲稿（备注）`/`DOCX 讲稿（备注）`。`presentation.js` 实例暴露 `scriptHtml()`。新建 `_notes_export_test.js` **14/14**。
- **② 图表导出 DOCX/XLSX（B）**：`export-ooxml.js` 新增 `colName/parseRange/buildChartXml/buildChartPartsForXlsx`（原生 OOXML 图表，引用单元格区域 `Sheet1!$B$2:$B$4`，bar/line/pie；drawing 用 `r:id` 引用、字面 `../charts/chartN.xml` 写在 `xl/drawings/_rels/drawing1.xml.rels`）。`buildXlsx` 接入 drawing+chart 部件+Content_Types+sheet rels。`spreadsheet.js` HTML 导出把图表以内联 SVG（`<figure>`）嵌入。XLSX 原生 + HTML SVG 完成；**DOCX 原生图表导出暂缓**（Writer/演示无图表数据，需内嵌子 xlsx，性价比低）。测试坑：`drawing1.xml` 不存字面路径 → `_chart_export_test.js` 改查 `.rels` 文件，最终 **15/15**。
- **③ 演示切换动画（C）**：`presentation.js` 每页 `transition{type,duration}`，`TRANSITIONS`(无/淡入/推进/擦除/缩放/翻转)、`TRANSITION_DURATIONS`(300/500/800)；`applyTransition(slideEl,tr,dir)` 用 CSS 关键帧 `tr-<type>[-rev]`（推进/擦除区分上下页方向），仅动画进入页、无需保留旧页；放映 `showPresenter` 的 `draw()` 按 `navDir` 应用本页 transition，编辑区 `previewTransition()` 预览。`ribbon` 新增「切换」tab（效果/时长下拉 + 预览 + 应用到全部）；`setTransition({type,duration,all})` 会剔除 `all` 键再存。`style.css` 加 `@keyframes tr-*` + `prefers-reduced-motion` 关闭。新建 `_transition_test.js` **17/17**（每实例独立 fresh slides，避免 `all:true` 污染共享 doc）。
- **④ Writer 目录 / 分页符（D）**：`writer.js` 新增 `buildTocHtml()`（扫描 h1-h3，自动补 `id=toc-h-N`，生成 `<nav class="toc" contenteditable=false>` 含 `.toc-link[data-target]`）、`insertNodeAtCaret(frag)`（Range 插入，兼容无 execCommand；`frag.lastChild` 需在 insertNode 前留存，否则 setStartAfter 抛「节点无父」）、`insertToc()`、`insertPageBreak()`（`<div class="page-break" data-page-break>`）。`page` 点击劫持 `.toc-link` → `getElementById(target).scrollIntoView` + 光标落标题。`icons.js` 补 `toc`/`page-break`；ribbon 插入 tab 加「页面/目录」组（分页符/目录）。`export-ooxml.js` `buildDocx.blockToParagraphs` 新增 `.page-break`→`<w:br w:type="page"/>`、`.toc`→标题+`w:hyperlink w:anchor` 列表，保证 DOCX 往返保真。`style.css` 加 `.toc*`/`.page-break*`（屏显虚线、`@media print` `break-after:page` 且隐藏标签）。新建 `_toc_test.js` **16/16**。
- 回归：_page_test 28/28、_notes_export_test 14/14、_chart_export_test 15/15、_transition_test 17/17、_toc_test 16/16 全绿。预览服务根目录 app/（端口 8111，跨轮需重启）。

