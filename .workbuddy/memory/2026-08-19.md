# 2026-08-19 工作日志

## 绿角犀 Office · 工作交接明细（用户指令）

用户：「做个日志和工作交接明细」——生成本次会话的工作移交文档，沉淀 30 轮「继续工作」状态与剩余待办。

### 交付物

- `.workbuddy/handoff-2026-08-19.md`（独立交接文档，约 280 行）：
  - §0 交接说明 / 读者 / 状态符号
  - §1 当前项目健康度（一句话 + 健康度维度表）
  - §2 30 轮工作回顾（轮次 → 关键产物 → 测试增量 → 套件累计）
  - §3 工程现状（技术栈 / 单一真源 / 测试门禁 / 模块契约）
  - §4 完成度表（按模块 + 状态 + 关键产物 + 测试）
  - §5 测试覆盖（套件清单 + 断言数）
  - §6 残留缺口（🔴 大型 / 🟡 进行中 / ⚠️ 已记录非阻断）
  - §7 第 31 轮进行中的工作（未完成清单 + 接手建议）
  - §8 关键工程纪律（8 条不可破坏）
  - §9 下一步推进建议（A–F 优先级）
  - §10 环境与工具（Node / Python / 关键命令 / npm 脚本 / CI 入口）
  - §11 关键文件 / 模块索引（树状图）
  - §12 备注 / 风险 / 后续行动

### 关键事实快照（截至 2026-08-19）

- **基线**：`npm test` 整链 **43 套件 0 失败** + 四端同源 ✅（昨日第 30 轮收口后建立）
- **第 31 轮工作中**：PDF→TXT/Markdown 导出
  - 完成：#238（`pdf-convert.js` 扩展就绪 `pdfToText` / `pdfToMarkdown` + api 暴露）、部分 #237（icons 加 txt/md 图标）
  - 未完成：#237 余项（pdf.js 工具栏按钮 + `exportAsText` / `exportAsMarkdown`）、#240（`_pdf_convert_text_test.js`）、#239（FEATURES 第 74 行文档漂移修复 + 顶部 43→44 + 同步四端 + 全量验证）
  - 中断原因：API 频率限制到达
- **剩余 🔴 大型**：PDF→Excel 骨架、docx→PDF 程序化（依赖需放宽）
- **剩余 🟡 边界**：PDF→DOCX/TXT/MD 版面还原有限、FEATURES 第 74 行漂移、MindMap 多模块覆盖深度未系统回测
- **非阻断 ⚠️ 已记录**：OFD spreadsheet 导出抛错（合理）、并行 `_auth_backup_test` 偶发抖动、MindMap 可编辑对象、MindMap↔OOXML/OFD 未对接

### 假设与风险

- 本文档为「状态索引」而非产品说明书：详尽实现以 `app/` 代码 + `FEATURES.md` + 各 `*.md`（CLIENTS_BUILD / UPDATER / STORE_SUBMISSION / CLOUD / AGENTS）为最终真源。
- 接手方首选：先完成第 31 轮收口（30 分钟），再按 handoff §9 序推进。
- API 频率限制：长会话分多段或换模型变体；接手后首先跑 `npm test` 取得本会话基线。

### 剩余（如有中断未完）

- 第 31 轮 4 项任务（#237 余项 / #240 / #239）——handoff §7 已列清单与代码模板位置

---

## 文档治理重构（用户指令，本日另一次任务）

用户确立文档治理原则：模块职责单一 / 主目录只导航 / 每个模块必有总纲（目的·子文件·状态·关系）/ 先总纲后细节 / 讨论与正式分离 / 导航文件只指路不堆剧情。

### 落地产物
- `工程总纲.md`：范围与导航单一真源（目的/模块地图/技术栈/状态/关系/治理规范）。
- `模块/`：8 个模块各带 `总纲.md`（01 文档编辑 / 02 PDF / 03 格式兼容 / 04 账户云端 / 05 外壳通用 / 06 客户端交付 / 07 发版更新 / 08 测试质量）；子文件内容本轮仅规划、待建。
- `讨论与处理/`：`流程说明.md`（固定流程）+ `_模板/专题模板.md`（推演→确认→回写）。
- `STATUS.md` / `MEMORY.md` / `CHANGELOG.md`：均只「指路 + 状态」。
- `AGENTS.md`：收敛为「运作宪法 + 导航指针」，原 §8 项目简介 / §9 清单迁入 `工程总纲.md`。

### 约定
- 后续任何功能/文档改动：先判归属模块 → 落模块总纲/子文件；争议先走 `讨论与处理/` 专题，确认后回写。
- `FEATURES.md` 仍为功能完成度明细真源；根级 `CLIENTS_BUILD.md / UPDATER.md / STORE_SUBMISSION.md` 待按治理迁入 `模块/07_发版与更新/` 子文件（避免重复维护）。
