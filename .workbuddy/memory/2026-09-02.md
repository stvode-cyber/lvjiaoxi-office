# 2026-09-02 工作日志

## 收口「１。２先做」① 的后续（PDF 1.5+ ObjStm 已全绿 + commit 9e32df0 / tag v1.0.16）

- **Android 构建中间件移出 git（commit `dbacf83`）**：`git rm --cached -r --ignore-unmatch clients/android/app/build` 把 21 个 M/D 跟踪项移出索引；磁盘文件保留，`.gitignore` 第 42 行已有 `clients/android/app/build/` 安全网，杜绝再误跟踪。`clients/android` 仍保留源码（keystore/Native 源码等），仅删 build 输出。
- **发布预检（受阻）**：本环境存在 VPS 私钥 `~/.ssh/id_ed25519_rcprod`（411B），feed URL 在 `app/version.json` 指向 `https://lujax.fun/releases`（正确，electron/feed-config.js 走 generic provider）。但 **沙箱到 VPS `8.149.245.252:22` 连接超时（Connection timed out，exit 255）**——无网络出口，发布上传无法在本环境执行。须在能连 VPS 的机器跑 `bash scripts/publish-latest.sh`（脚本自动设 host=8.149.245.252/user=root/key=`~/.ssh/id_ed25519_rcprod`，并 SSH `find` 定位远端 releases 目录）。
- **Windows 安装包构建**：
  - 上次后台构建 `2m5zgs`（PowerShell 包装）中途挂死——日志停在 `packaging platform=win32 arch=x64 electron=31.7.7`，无进程存活（僵尸状态），未产出 1.0.16 产物、未写 `BUILD_EXIT`。已 `TaskStop` 清理（killed）。
  - 排查：磁盘 413G 空闲充足，win 配置正常（nsis+portable x64，output=dist），非 OOM/配置问题；判为 PowerShell 后台包装进程被回收。
  - 改用 **Bash 后台通道**重启（task `sd3POR`，`NODE_OPTIONS="" npx electron-builder --win --publish never`）；`win-unpacked` 已缓存 210MB（Electron 无需重下），预期显著快于首次。完成将自动通知。
- **当前全链路状态**：① ObjStm 合并拆分 ✅ 已交付（87 套件 0 失败 / commit 9e32df0 / tag v1.0.16 / 四端同源）；② FEATURES.md 自动更新文档（前序会话完成）；Android 清理 ✅；构建 ⏳ 后台中；发布 ⚠️ 受沙箱网络限制（须用户侧机器执行一行命令）。

## 下一步（待)
1. 构建 `sd3POR` 完成后：校验 `dist/` 根级 `Setup 1.0.16.exe`+`latest.yml`+`.blockmap`（含 portable），删 `build_raw.txt`。
2. 发布（用户侧机器）：`bash scripts/publish-latest.sh` 或 `LVJX_FEED_SSH_KEY="$(base64 -w0 ~/.ssh/id_ed25519_rcprod)" node scripts/publish-desktop-feed.js --latest-only` → 旧客户端拉 latest.yml 走应用内自动更新。
3. git remote 配好后 `git push && git push --tags`（v1.0.0~v1.0.16 本地，未 push）。

## AGENTS 元框架 + 首张 FD 实例（14:18）
- **FD 节点写入 `D:\Obsidian\AGENTS.md`**：项目层「功能清单 F」下新增 `## 功能开发记录 FD（每功能一记 · 可溯源）`，进 R-项目 索引。字段固定：说明 / 注释（细节·坑）/ 目标（G/VG）/ 关联（↔E·↔Dc·↔A）；落位 `项目内 FD/ 目录`，命名 `FD-<功能短名>.md`，关联用 `<节点>-<日期>-<短标识>` 回查。
- **首张 FD 实例**：`FD/FD-objstm.md` —— 把「PDF 1.5+ ObjStm 合并/拆分」落卡：说明（1.5+ 对象流解析展开）/ 注释（async parsePdf、extractStreamData 剥 EOL、extractFirstBalancedDict 含尾 `>`、偏移相对 First）/ 目标（G=PDF 全特性边界；VG=四端同源）/ 关联（↔E 两解析 bug · ↔Dc pdf-async · ↔A commit 9e32df0 / tag v1.0.16 / 12 断言）。作为模板，后续每功能一记。
- **FD 回填（「1。2。3」之①）**：按 STATUS 模块清单 01–08 回填 8 张模块级 FD 卡（`FD-01-doc-edit` ~ `FD-08-test`），+ `FD-objstm` 专项 = **共 9 张**。关联字段只填可验证引用（ObjStm `9e32df0`、模块总纲路径、bump 1.0.3/1.0.15），不臆造旧 commit 哈希。
- **② 发布校验**：`dist/` 根级 1.0.16 产物齐全（`Setup 1.0.16.exe` 78.9MB + `绿角犀 Office 1.0.16.exe` portable 78.7MB + `.blockmap` 83KB + `latest.yml`），待用户侧机器 `bash scripts/publish-latest.sh` 上传 VPS（沙箱连不上 VPS，已证）。
- **③ git push 校验**：本地 **17 个 tag**（v1.0.0~v1.0.16）、**无 remote**；push 须先 `git remote add origin <url>` 再 `git push --tags`。
- **发布包装 `.bat`（15:46）**：新增 `scripts/publish-latest.bat`（Windows 双击即跑，`cd` 到项目根后调 `bash scripts/publish-latest.sh`，带 bash 缺失检测 + 退出码提示）。把 ② 用户侧那步从"开终端敲命令"降为"双击"。沙箱仍无法代跑（VPS 全端口死）。③ 仍仅缺 remote URL（若 github/gitee 等代理可达主机，沙箱可当场 push）。
