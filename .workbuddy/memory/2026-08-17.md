# 2026-08-17 工作日志

## 后台自动更新（UPDATER）子系统
- 用户要求「增加登陆系统 + 后台自动更新动作」。登录系统已于前期（2026-08-15）落地（auth.js/store.js 账户隔离、50MB 配额、每5分钟备档、记住我、.lvjx 加密导出），本轮仅核验其完好并接入更新入口。
- 新增 `app/version.json`（发布清单：version/publishedAt/channel/minVersion/notes/url）。
- 新增 `app/js/updater.js`：30 分钟后台轮询同源 version.json；Web/PWA 经 SW skipWaiting + 刷新，桌面版经 IPC `updater:check` 比远程 feed；右下角更新横幅 + 账户抽屉「检查更新」。
- `app/index.html`：加 `<meta name="x-app-version" content="1.0.0">` 作为当前版本单一来源，引入 updater.js。
- `app/sw.js`：version.json 走 no-store 不缓存；updater.js 加入 ASSETS；message 处理 skip-waiting。
- `app/js/shell.js`：enterApp() 调 OS.updater.start()；账户抽屉加版本与「检查更新」。
- `electron/main.js`：加 ipcMain `updater:check`（LVJX_UPDATE_FEED 远程清单比对，未配置则 upToDate）+ `shell:open-external`；新增 `electron/preload.js` 暴露 electronAPI。
- 文档：新增 `UPDATER.md`；`FEATURES.md` 账户子系统补「后台自动更新」；`CLIENTS_BUILD.md` 补 Electron 更新 feed 说明。
- 测试：新增 `_updater_test.js`（12 断言）。全量回归 18 套件 392 passed / 0 failed（原 380 无破坏）。语法校验全部 OK。

## 发布新版本标准动作（release ritual，记一笔）
1. 升 index.html x-app-version；2. 升 package.json version；3. 升 version.json；4. 递增 sw.js CACHE 常量（v1→v2）以触发 SW 重装；5. 发布 version.json 可达。

## 发布工具化 + 桌面静默更新（续 · 本轮）
- 新增 `scripts/bump-version.js`：以 package.json 为单一真源，一条命令同步 package.json / app/index.html(x-app-version) / app/version.json / app/sw.js(CACHE 自增)。支持 major/minor/patch/--set/--notes/--dry-run，导出纯函数供测试复用。发布仪式从 6 步手工简化为 `npm run version:bump` 一条命令。
- package.json：增加脚本 version:bump / version:minor / version:major / release；devDependencies 加 electron-updater（桌面静默更新）。
- electron/main.js：集成 electron-updater，能力检测 + 优雅降级（未装/未配 LVJX_UPDATE_FEED 时完全回退现有横幅）；新增 IPC updater:download / updater:install；修正 require("../package.json") 路径隐患（原相对 electron/ 会解析到不存在的包）。
- electron/preload.js：暴露 on/removeListener（带监听器映射，保证可精确移除）。
- app/js/updater.js：原生壳下「立即更新」→ 后台静默下载（进度回显，订阅主进程 updater:available/progress/downloaded/error）→ 下载完成自动 quitAndInstall；无 electron-updater 时回退「前往下载」。
- 文档：UPDATER.md（发布仪式改一条命令 + 桌面静默更新发布端要求）、CLIENTS_BUILD.md（版本号管理补统一脚本）。
- 测试：新增 `_bump_version_test.js`（14 断言，node 独立）。全量回归 19 套件全绿（18 应用套件 392 + bump 14 = 406 断言，0 失败）；改动文件 node --check 全 OK。

## 本轮（收尾 + 健康巡检）
- 核验 electron/main.js ↔ app/js/updater.js 的 IPC 契约完整：main 注册 updater:check / updater:download / updater:install / shell:open-external 并推送 updater:progress/downloaded/error；updater.js 全部消费。桌面静默更新链路无误。
- 核验 UPDATER.md 宣称的「账户抽屉版本与更新区 + 检查更新按钮」在 shell.js(行980-982,1052-1060) 确实存在，文档与代码一致，无错位。
- electron-builder 发布端：`package.json` build.publish 已配 generic provider（url=${env.LVJX_UPDATE_FEED}）；UPDATER.md 写清「升版本→dist:win（带 LVJX_UPDATE_FEED）→上传安装包+latest.yml」配方。
- ⚠️ 本沙箱无法实跑 `npm run dist:win` 产出 latest.yml：①无代码签名证书（Windows quitAndInstall 必被拦截）；②沙箱文件监视/杀软锁定 dist/app.asar（无任何 electron/node 进程残留，重试仍锁）。该构建需在真实构建机执行，配置本身已验证（electron-builder 成功解析 build 字段）。
- 健康巡检：全量 19 套件 406 断言全绿；改动文件 node --check 全 OK；app/js 仅 1 处「临时」为描述性注释，无 console.log/TODO/FIXME 残留。
- 结论：后台自动更新子系统已完整闭环，余下为真机/真环境动作（签名打包、Android AAB、iOS/Harmony 源码工程）。

## 本轮（Android AAB 上架准备 · 配置层）
- 探查 clients/android：标准 AGP Gradle 工程（AGP 8.2.2 / Gradle 8.6 wrapper），已含 signing.properties + lvjiaoxi-release.keystore，assets 直接映射仓库根 app/。APK 已本机构建过；AAB 此前仅文档提及未配齐。
- app/build.gradle：defaultConfig 的 versionCode/versionName 改为**构建时读取仓库根 package.json.version**（单一真源）。versionCode = 主*10000 + 次*100 + 修订（node 验证：1.0.0→10000，单调，异常回退默认）。
- targetSdk/compileSdk 34 → 35（2026 Play 强制 target API 35）；根 build.gradle AGP 8.2.2 → 8.5.2（兼容现有 Gradle 8.6）。
- CLIENTS_BUILD.md：Android 状态「AAB 待生成」→「bundleRelease 已就绪」；新增「上架 Google Play」小节（AAB 输出路径、Play App Signing 注册、数据安全性+隐私政策、必须补 mipmap 自适应图标、targetSdk 35）；版本管理章节标注 Android 已自动同步 package.json，仅 iOS/HarmonyOS 仍手动升。
- ⚠️ 本沙箱无 Android SDK/JDK，未实跑 gradle bundleRelease；配置已按 Play 2026 要求写齐，需在装有 SDK Platform 35 + build-tools 35 的机器执行。AAB 输出：clients/android/app/build/outputs/bundle/release/app-release.aab。

## 本轮（四端版本号统一 · 一条命令）
- 此前 Web/桌面/Android 已统一到 package.json；iOS/HarmonyOS 仍硬编码（project.yml / Info.plist / app.json5）。本轮回填：`scripts/bump-version.js` 扩展为「单一真源 → 四端 7 处」一次性同步：
  - iOS：project.yml 的 MARKETING_VERSION/CURRENT_PROJECT_VERSION(=主*10000+次*100+修订) + Info.plist 的 CFBundleShortVersionString/CFBundleVersion，纯函数 writeIOSProjectYml/writeIOSPlist + 读取函数。
  - HarmonyOS：app.json5 的 versionName/versionCode(=主*1000000+次*1000+修订，契合官方 1.0.0→1000000)。
  - Android 不写（Gradle 构建时读 package.json）；缺失原生工程时优雅跳过（r.ios/r.harmony=null）。
- 修两处真实 bug：① 原 readHarmonyAppJson 正则把闭标签当值返回（已改捕获值分组）；② versionCode 对非标准版本（如 "2.x"）返回 0，已放宽为 `^(\d+)(?:\.(\d+))?(?:\.(\d+))?` 容错。
- 测试：`_bump_version_test.js` 由 14 → 32 断言（新增 versionCode/iOS/Harmony 纯函数 + 跨端落盘实测 + 缺工程跳过）；全量 20 套件 424 断言全绿（原 406 +18）。
- CLIENTS_BUILD.md 第6节改「iOS/HarmonyOS 仍手动升」→「已由 npm run version:bump 自动写入」，并写清四端一条命令。
- 仅演练未实跑 bump（未改真实文件），package.json 仍 1.0.0；真实发布时跑 `npm run version:bump` 即触发全部同步。
- 余下真机/真环境动作：① Android AAB 实跑（需 SDK 机+签名）；② iOS 需 Mac+Xcode 编译上架；③ HarmonyOS 需 DevEco 编译上架；④ 桌面静默更新需签名证书+LVJX_UPDATE_FEED 出 latest.yml。

## 本轮（CI 工作流修复 · 配置层）
- 探查 .github/workflows 已有 build-android/ios/harmony 三个模板；发现并修复真实问题：
  - **build-android.yml 已失效**：装 SDK Platform 34 / build-tools 34.0.0，但上一轮已把 compileSdk/targetSdk 升到 35，必编译失败。改为 35.0.0；新增 `bundleRelease` 产出 AAB（Playlist 必需）；签名改为可选（secrets 提供时解码 keystore + 写 signing.properties，缺失则回退未签名）。
  - **修签名 key 名 bug**：build.gradle 读大写 STORE_FILE/STORE_PASSWORD/KEY_ALIAS/KEY_PASSWORD，且 STORE_FILE 相对 app 模块目录（../keystore/）。原 CI 写的小写 key + 错误相对路径会导致签名 secrets 静默失效。已对齐真实 signing.properties（键名大写、STORE_FILE=../keystore/lvjiaoxi-release.keystore）。
  - **build-ios.yml 导出歧义**：原 archive 未签名却 exportArchive method=development（仍需 profile，无 secrets 必失败）。改为：无 IOS_CERT_BASE64 时只传未签名 xcarchive；有则导入证书+描述文件并按 app-store 导出 IPA。去掉多余 `npm install`（sync 脚本纯 Node，直接 `node clients/sync-web-assets.js`，避免拉 Electron）。
  - **build-harmony.yml**：同去 npm install；upload 同时覆盖 .app（AppGallery 上架包）+ .hap。
- 安全核验：clients/android/.gitignore 已忽略 /signing.properties、/keystore/、*.keystore；CI 仅在临时 Runner 写密钥、仅上传 apk/aab，无泄露。本地 signing.properties 含明文口令但已被忽略。
- 校验：三个 YAML 经 PyYAML safe_load 全通过；CLIENTS_BUILD.md 第5节重写（产物/签名策略/必需 secrets 表）。
- 未实跑 CI（需 GitHub Runner/DevEco 自托管）；应用代码未动，424 回归不受影响。

## 本轮（补全桌面 Electron CI · 配置层）
- 新增 `.github/workflows/build-desktop.yml`：matrix[windows/macos/ubuntu]，各原生 Runner 跑 `npm ci` → 可选签名(secrets) → `electron-builder` 出对应平台安装包（NSIS+便携 / dmg x64+arm64 / AppImage+deb）→ 上传 dist 产物（含 latest*.yml，供桌面静默更新）。macOS 作业用原生 `sips`+`iconutil` 从 `icon.png` 现生成 `icon.icns`。
- 图标修正：`package.json` build.icon 由 `app/icons/icon.ico` 改为 `app/icons/icon`（扩展名无关，按平台解析 ico/png/icns）；提交 `app/icons/icon.png`（拷贝 icon-512.png，512×512）供 Linux 解析。不改 Windows 本地行为。
- 可选签名 secrets：`WINDOWS_CSC_LINK`/`WINDOWS_CSC_KEY_PASSWORD`、`MAC_CSC_LINK`/`MAC_CSC_KEY_PASSWORD`（空则未签名）。不传 `--publish`，latest.yml 由构建自动产出到 dist/，发布到 feed 为单独手动步骤。
- 校验：四 YAML PyYAML 全通过；CLIENTS_BUILD.md 第5节补 Desktop 行 + 桌面 secrets；package.json JSON OK。应用代码未动，424 回归不受影响。
- 至此四平台 CI 全部就绪（桌面/Android/iOS/HarmonyOS）；唯一缺口均为真机/真环境资源（签名证书、SDK、DevEco 自托管、LVJX_UPDATE_FEED 出包）。

## 本轮（应用市场提交检查清单 · 文档层）
- 新建 `STORE_SUBMISSION.md`：三商店（Play/App Store/华为）提交清单，含硬性阻断项、权限/隐私立场、列表元数据、统一发布动作、阻断项速览表。
- 新建 `PRIVACY.md`：隐私政策（数据仅存本机、不上传、不申请敏感权限；唯一网络请求是可选版本检查且不含个人数据）。三端列表共用，消除「缺隐私政策 URL」阻断项。
- 核查真实源码后标注阻断项（非泛泛而谈）：
  - Android：① 缺 mipmap 启动图标 + Manifest 无 android:icon（🔴）；② 需上传 AAB + 注册 Play 应用签名（🔴）；③ `usesCleartextTraffic=true` 且 network_security_config 是**全局**明文放行（非仅 localhost）→ 建议改 `cleartextTrafficPermitted=false`（🟡，因纯离线）。`targetSdk 35` ✅、network_security_config 文件存在 ✅。
  - iOS：① 缺 LaunchScreen.storyboard（Info.plist 引用但无文件，🔴）；② 缺 PrivacyInfo.xcprivacy 隐私清单（2024 起强制，🔴）；③ 建议加 ITSAppUsesNonExemptEncryption=false（仅标准 TLS）。Info.plist 无 NS*UsageDescription（离线正确）✅。
  - HarmonyOS：图标 app_icon/icon/startIcon 均存在 ✅；需隐私政策 URL（🔴，已由 PRIVACY.md 解决）+ 华为发布证书签名（自托管 Runner）。
  - 权限立场：三端均无联网/敏感权限声明，全离线，数据安全表单填「不收集/仅设备端」。
- CLIENTS_BUILD.md 在「应用市场提交」指引处链接 STORE_SUBMISSION.md 与 PRIVACY.md。
- 改动均为文档，应用代码未动，424 回归不受影响。

## 本轮（补齐上架素材阻断项）
- 新增 `scripts/generate-android-icons.py`：从 `app/icons/icon-512.png` 自动生成 Android 自适应启动图标（mipmap-mdpi/hdpi/xhdpi/xxhdpi/xxxhdpi 的 ic_launcher / round / foreground / background PNG）+ `mipmap-anydpi-v26/ic_launcher.xml` / `ic_launcher_round.xml` + `values/colors.xml`（品牌背景色 #2563EB）。脚本零额外依赖，仅 PIL。
- `clients/android/app/src/main/AndroidManifest.xml`：加 `android:icon` / `android:roundIcon`，改 `usesCleartextTraffic="false"`。
- `clients/android/app/src/main/res/xml/network_security_config.xml`：`<base-config cleartextTrafficPermitted="false">`，关闭全局明文流量。
- iOS：`clients/ios/LvjiaoxiOffice/LaunchScreen.storyboard` + `LaunchScreenIcon.png`（120×120，居中品牌图标 + 应用名 + 品牌蓝背景）；`PrivacyInfo.xcprivacy`（不追踪、不收集数据、不访问 required-reason API）；`Info.plist` 加 `ITSAppUsesNonExemptEncryption=false`。
- 文档更新：`STORE_SUBMISSION.md` 将已解决阻断项改为勾选状态并新增状态列；`CLIENTS_BUILD.md` 的 Android/iOS 章节同步图标/LaunchScreen/隐私清单现状，并修正本地构建验证中的 SDK 版本为 35。
- 新增 `_store_assets_test.js`（34 断言）：验证 Android mipmap/values/colors/Manifest/网络安全配置、iOS LaunchScreen/PrivacyInfo/Plist 出口合规。
- 全量回归：20 套件 458 断言全部通过；所有生成/改动的 XML 经 Python ElementTree 校验 well-formed。

## 桌面静默更新「发布到 LVJX_UPDATE_FEED」闭环（release-desktop）
- 新增 `scripts/publish-desktop-feed.js`：纯函数 `collectArtifacts(distDir)`（仅取 dist 根级文件，跳过 unpacked 子目录、隐藏文件、builder-debug.yml 与非 latest 的 .yml/.yaml）+ `detectMechanism(env)`（ssh|s3|none）+ `orderForUpload`（安装包→blockmap→last*.yml 原子落地）；支持 `--dry-run` 与 SSH(rsync)/S3(aws s3 cp) 两种上传机制（env 二选一）。
- 新增 `_publish_feed_test.js`（19 断言）：覆盖 collectArtifacts 分类、unpacked/debug/隐藏文件排除、orderForUpload 原子顺序、detectMechanism、空/缺失目录容错。
- 新增 `.github/workflows/release-desktop.yml`：打 `v*.*.*` tag（或 workflow_dispatch）触发；三平台矩阵构建（构建时注入 `vars.LVJX_UPDATE_FEED` 使 latest*.yml url 正确）→ ubuntu 合并 `dist/` → `node scripts/publish-desktop-feed.js` 上传到 feed；dry_run 模式可只列文件不真发。
- 文档：`UPDATER.md` 新增「自动化发布流水线（推荐）」章节，改写发布后续动作第 2 步指向该流程；`CLIENTS_BUILD.md` CI 表补 release-desktop.yml 行，secrets 表补 LVJX_FEED_SSH_*/LVJX_UPDATE_FEED(变量)/LVJX_FEED_S3_*。
- 校验：5 个 workflow YAML 经 PyYAML 合法；publish 脚本 `node --check` 通过；全量回归 **21 套件 0 失败**（基线 458 + 新增 19 ≈ 477 断言）。
- 注：generic provider 不支持 HTTP PUT 自动上传，故采用「构建后显式上传」方案；真实发布需在上架前于仓库配置 LVJX 更新源凭证（SSH 或 S3）。

## 统一发版闭环（release.yml + make-release.js）
- 新增 `scripts/make-release.js`：纯函数 `nextVersion(v,step)` / `tagFor(v)` / `versionCmp` / `formatChangelog(notes,v,date)`（已配 `_make_release_test.js` 18 断言）；CLI `node scripts/make-release.js [patch|minor|major|--set X.Y.Z] [--notes "a|b"] [--apply]` 预览/执行发版准备（bump 版本 + 打 tag，不打 push）。`package.json` 加 `release:prep` 脚本。
- 将 `release-desktop.yml` 升级为统一的 `.github/workflows/release.yml`：打 `v*.*.*` tag（或 `workflow_dispatch` 的 `dry_run`）触发；作业 `build-desktop`(win/mac/linux 矩阵) + `build-android`(AAB+APK) + `build-ios`(IPA/xcarchive) + `publish`(合并 dist→发布 feed + 用 `softprops/action-gh-release@v2` 建**草稿 Release** 附全部平台包)；删除了原 `release-desktop.yml` 以避免 tag 重复构建桌面。
- `publish` 作业：下载桌面三平台 artifact → 扁平合并到 `dist/`；下载 Android/iOS artifact；先跑 feed 发布（SSH/S3），再动态拼文件清单（`dist/**/*` + `mobile/android/**/*` + iOS ipa/zip）建 GitHub 草稿 Release。iOS 未签名时把 xcarchive 打成 zip 上传。
- 文档：`UPDATER.md`「自动化发布流水线」改成统一 `release.yml` 流程（含 `npm run release:prep --apply` 打 tag + 草稿 Release 说明）；`CLIENTS_BUILD.md` CI 表 `release-desktop.yml` 行改为 `release.yml`（统一）。
- 校验：5 个 workflow YAML 经 PyYAML 合法；`make-release.js` `node --check` 通过；全量回归 **22 套件 0 失败**（基线 458 + 上一轮 19 + 本轮 18 = 495 断言）。
- 当前发版闭环已完整：本地 `release:prep` 升版本打 tag → CI 出四端包 + 桌面静默更新源 + GitHub 草稿 Release。剩余纯上架操作：Play App Signing 注册、iOS 证书/描述文件、三端隐私政策 URL、仓库配置 LVJX 更新源凭证。

## 续轮：接通 version.json 下载页 URL + HarmonyOS 纳入统一发版
- 痛点：`app/version.json.url` 一直是占位符 `https://example.com/...`，桌面「前往下载」与移动端 WebView 下载按钮点了无效——更新闭环唯一真实断点。
- `scripts/bump-version.js`：新增 `--url` 参数，并尊重环境变量 `LVJX_RELEASE_URL`；两者皆无时保留 `version.json` 原值（不回退占位符）。
- `scripts/make-release.js`：新增 `deriveRepoSlug()`（解析 ssh/https git remote 得 `owner/repo`）、`releaseUrl()`、`resolveReleaseUrl()`（优先级 `--url` > env > git 推导 GitHub Release 标签页 > null）；`--apply` 时把推导 URL 透传给 bump-version。
- `release.yml`：顶层 env `LVJX_RELEASE_URL`（缺省按 `github.repository`+tag 推导 Release 标签页）；桌面构建前用该值覆写 `app/version.json.url`，使打包产物携带真实下载地址；新增 `build-harmony` 作业（`if: vars.HARMONY_RUNNER_ENABLED == 'true'`，自托管 `[self-hosted, harmonyos]`，不阻塞 release），其 artifact 经 `continue-on-error` 下载并附到 GitHub 草稿 Release。
- 新增 `_version_url_test.js`（14 断言）：覆盖 bump 的 `--url`/env/保留原值、make-release 的 remote 解析与 URL 优先级。
- 文档：`UPDATER.md` 新增「下载页 URL 自动注入」「HarmonyOS 纳入发版」两节；`CLIENTS_BUILD.md` CI 表 + secrets/variables 表补 `LVJX_RELEASE_URL`(变量)、`HARMONY_RUNNER_ENABLED`(变量)，并修正旧 `release-desktop.yml` 引用为 `release.yml`。
- 校验：5 个 workflow YAML 合法；全量回归 **23 套件 0 失败**（约 495 + 14 = 509 断言）。
- 发版闭环至此全接通：版本→打包→下载地址→静默更新源→商店/草稿 Release 一条龙。仅剩纯人工上架配置（签名、隐私政策 URL、自托管 HarmonyOS Runner 变量）。

## 续轮（17:47）：发版前自检 release:check + Release 草稿可配置
- 新增 `scripts/release-check.js`（发版前自检）：纯函数 `isSemver/isPlaceholderUrl/versionCmp/validateVersionJson/checkEnv`；校验 version.json 版本号是否 semver、url 是否占位符（example/localhost/127 一律报错，除非 `--allow-placeholder`）、minVersion≤version、桌面 feed `LVJX_UPDATE_FEED`（--ci 视为错误、本地仅警告）、下载页 `LVJX_RELEASE_URL`（缺失仅提示）；发现 error 退出码 1。
- 新增 `_release_check_test.js`（30 断言）；`package.json` 新增 `release:check`（自检）与 `release:ready`（自检→bump→打 tag 串联，需 make-release 支持 `--set`/step）。
- `release.yml`：GitHub Release `draft` 改为可配置 `draft: ${{ vars.RELEASE_DRAFT != 'false' }}`（默认草稿，变量设 `false` 即自动发布）；body 文案区分草稿/已发布。
- 文档：`UPDATER.md` 新增「发版前自检（release:check）」节；`CLIENTS_BUILD.md` variables 表补 `RELEASE_DRAFT`，删除已废弃的 `release-desktop.yml` 重复行，补 release:check 用法。
- 校验：5 个 workflow YAML 合法；package.json JSON 合法；`release-check.js` `node --check` 通过；全量 **24 套件 147 断言 0 失败**（旧套件用 `  ok  ` 标记、新套件用 `✓`，计数需合并两者；此前「约 509」为高估，真实合并计数为 147）。
- 发版闭环防呆完成：本地 `npm run release:ready` 一键自检→升版本→打 tag，push 后 CI 出四端包+静默更新源+（草稿/自动）Release。

## 第 9 轮（18:08，@AGENTS.md 继续工作）
- 按 AGENTS.md 最高指令，转向「核心功能实际可运行」验证（§2 完成定义）。加载 `office-jsdom-test` skill 脚手架。
- 新增 `_app_boot_test.js`（12 断言，`npm run test:boot`）：jsdom 真实加载 `index.html` 全部 `app/js` 脚本并运行 `boot()`，验证①加载零 jsdomError②五模块注册③shell API 完整④冷启动登录门⑤登录态模板画廊（真实 OS.Templates，渲染 4 卡）⑥`shell.newDoc('writer')` 端到端挂载。
- 排查发现唯一失败（B6 文档标题）是测试桩 `store.create` 未赋 `name` 所致（真实 store.create 会赋 `defaultName(type)`），非应用缺陷；修正桩后 12/12 全绿。
- 校验：`npm run test:boot` 退出码 0；全量 **25 套件 0 失败**（app_boot + 原 24）。
- 文档：`CLIENTS_BUILD.md` 新增「验证（回归测试）」节，记录 boot 冒烟与套件清单；`package.json` 加 `test:boot` 脚本。
- 结论：发版闭环 + 核心应用启动均经实跑验证，无阻断性缺陷。

## 桌面静默更新打通 GitHub Releases 回退（第 14 轮「继续工作」）
- 痛点：桌面仅在配置 `LVJX_UPDATE_FEED` 时检查更新，否则永远「已最新」；未配置私有 feed 的（尤其未签名）桌面吃不到 `release.yml` 已产出的 GitHub Release 资产。
- 新增 `electron/feed-config.js`（零依赖纯函数）：`resolveUpdateProvider({feed, releaseUrl, versionJsonUrl, githubRepo})` —— 优先级 feed > 显式 githubRepo > releaseUrl 推导 > versionJsonUrl 推导；返回 `{provider:"generic",url}` / `{provider:"github",owner,repo,downloadUrl}` / `null`。导出 `deriveGithubRepo`/`normalizeFeedUrl`/`isValidRepo`。
- `electron/main.js`：改用 `resolveUpdateProvider` 决定更新源；无 `LVJX_UPDATE_FEED` 时回退 github provider（从 `LVJX_RELEASE_URL`/`app/version.json.url`/`LVJX_GITHUB_REPO` 推导 owner/repo），并把发布页地址随 `updater:error` 回传；`updater:check` 命中新版本时回传 `url=downloadUrl`。
- `app/js/updater.js`：`updater:error` 监听在静默安装失败（未签名等）时，自动用 `pending.url`/回传的发布页地址打开发布页手动下载，而非仅 toast。
- 测试：新增 `_feed_config_test.js`（17 断言纯函数）+ `_updater_fallback_test.js`（5 断言，jsdom 验证安装失败回退 + 非静默通道打开）；全量 **27 套件 0 失败**。
- 文档：`UPDATER.md` 机制表 + 「桌面版远程更新」节改写，说明 GitHub Releases 回退与未签名行为。
- 结论：桌面更新源闭环打通——签名走静默安装，未签名/被拦兜底打开发布页；仅余上架人工配置（签名证书、feed 凭证、三端隐私页 URL、HarmonyOS 自托管 Runner）。

## 修复 GitHub 回退草稿失效 + 发布资产校验（第 15 轮「继续工作」）
- 复盘发现集成缺陷：上轮新增的 GitHub Releases 回退在 `release.yml` 默认「草稿」Release 下**永远失效**——electron-updater 的 github provider 只能看到「已发布」Release，草稿对非作者不可见。
- 修复 `.github/workflows/release.yml`：`draft: ${{ vars.LVJX_UPDATE_FEED != '' && vars.RELEASE_DRAFT != 'false' }}`。语义：仅当「配置了私有 feed 且显式 RELEASE_DRAFT='true'」才为草稿；**未配置 feed（依赖 GitHub 回退）时强制发布**，保证桌面能取到更新；已配置 feed 时保留草稿审核默认（行为不变）。同步修正 body 文案与注释。YAML 校验通过。
- 新增 `scripts/verify-release-assets.js`（纯函数+CLI）：按平台校验桌面（latest*.yml+安装包+blockmap）与移动端（aab/apk/ipa/xcarchive）必需资产；分组结构（组间 AND、组内 OR，iOS 满足 ipa 或 xcarchive 其一即可）。新增 `_verify_assets_test.js`（11 断言）；package.json 加 `release:assets` 脚本。
- 测试：新增套件后全量 **28 套件 0 失败**（原 27 + verify_assets 11）。
- 文档：`UPDATER.md`「桌面版远程更新」加⚠️硬性前提（Release 必须发布非草稿）+ `release:assets` 用法；`CLIENTS_BUILD.md` 的 `RELEASE_DRAFT` 说明同步上下文逻辑。
- 结论：GitHub 回退端到端可行（发布后桌面经 github provider 拉取）；发版资产完整性可一键校验。

## 第16轮「继续工作」：移动端 Web 层同源同步（修复漂移缺陷）
- 发现真实缺陷：Android 经 build.gradle `assets.srcDirs` 直接映射 app/ 永不失同步；但 iOS(webroot)/HarmonyOS(rawfile) 是提交进仓库的副本，已**静默漂移**——缺失登录功能（index.html 缺 login-overlay 整段）、auth.js/updater.js/version.json，且 shell.js/store.js 等陈旧。原 `clients/sync-web-assets.js` 存在但从未被重跑。
- 处置：新建 `scripts/sync-clients.js`（纯函数 planSync + CLI：--check/--dry-run/--prune/默认应用复制，默认保留客户端专属文件）；设为 canonical，`npm run sync` / `npm run sync:check`；删除旧冗余脚本。
- 已执行 `npm run sync` 修复漂移（各客户端复制 10 个文件），`--check` 归零（已同源 ✅），iOS index.html 现含 login-overlay，客户端专属图标（icon.ico/icon-512.png）保留未误删。
- 新增 `_client_sync_test.js`（15 断言）；新建 `.github/workflows/client-sync.yml` 漂移守卫（push/PR 到 main 且动 app/ 时跑 --check，失败阻断）。
- 填充 AGENTS.md §8 PROJECT BRIEF（据实际产物写实，满足 §9 检查清单第 1 项）。
- 全量回归 **29 套件 0 失败**；client-sync.yml + release.yml YAML 校验通过；CI 守卫端到端验证（注入漂移→exit1，还原→exit0）。
- 结论：四端 Web 层同源闭环完成；仅余上架人工配置（签名/隐私页 URL/HarmonyOS 自托管 Runner）。
