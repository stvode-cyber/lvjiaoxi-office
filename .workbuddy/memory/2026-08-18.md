# 2026-08-18 工作日志

## 绿角犀 Office · 云端后端（补齐 PRD 核心缺失）

第 17 轮「继续工作」——发现并修复 PRD 核心功能缺口：云端账户后端此前仅为前端骨架（`auth.js` 定义 `/api/*` 契约但无服务端），故「50M 个人云空间」只是本地 IndexedDB 模拟、「自动备份」清浏览器数据即全丢。

### 交付物
- `server/index.js`：零依赖 Node 服务，同时静态托管 `app/` + 实现 `/api/auth/*`（scrypt 密码哈希 + HMAC 签名 token，30 天）、`/api/docs`、`/api/backups`、`/api/space`；服务端强制 50MB 配额（超限 413）；按用户文件存储 `server/data/<user>.json`；目录穿越守卫。仅 main 时自监听，导出 `start()` 便于测试。
- `app/js/cloudsync.js`：`OS.CloudSync`，云端会话激活；登录时双向合并（拉云端缺失/更新 + 推本地缺失/更新）、每 60s 兜底同步、备档周期同步；网络/超时/配额满静默降级不阻塞 UI；`start()` 现返回首次同步 Promise 便于 await。
- `app/js/store.js`：新增公开 `putBackup(snap)`（云端拉取/迁移写入，不触发自动 prune）。
- `app/index.html` + `app/js/shell.js`：载入 cloudsync；云端登录 `start()`、登出 `stop()`、备档周期 `syncNow()`；账户抽屉展示云同步状态。
- `package.json`：新增 `cloud` / `cloud:test`；`.gitignore` 排除 `server/data/` 与 `_*_result.txt`。
- `CLOUD.md`：自托管部署说明；`FEATURES.md` 云端账户由 🟡 升 ✅，已知事项补充云端恢复。

### 验证
- `_cloud_server_test.js` 25/25（起真实服务覆盖全部端点 + 配额 413 + 静态 + 穿越守卫）。
- `_cloudsync_test.js` 16/16（纯函数：shouldSync/mergePlan/端点拼接/鉴权头）。
- `_cloudsync_integration_test.js` 7/7（真实 server + 驱动 `OS.CloudSync`：本地→云端上传、云端独有→本地拉取、配额满 graceful）。
- 全量 **32 套件 0 失败**（上轮 29，+3）。
- 改动文件 `node --check` 全 OK。

### 假设与风险
- 假设「云端账户」即 PRD「内置认证 + 个人云」的落地形态；本地账户仍离线优先、永不联网，与「数据不出域」互不冲突。
- 风险：默认 `LVJX_CLOUD_SECRET` 为开发占位，生产必须设置强密钥（CLOUD.md 已警示）。
- 风险：跨设备同步为「按 id 去重 + updatedAt 较新者胜」的简易双向合并，未处理并发编辑冲突（原型阶段可接受，非阻断）。

### 剩余（均非代码）
- 签名证书（Win/macOS/Android Play/iOS）、三端隐私政策 URL、`LVJX_UPDATE_FEED` 凭证、HarmonyOS 自托管 Runner。
- 真实后端部署托管（当前为可自托管单机服务，未接公网/多实例）。

## 绿角犀 Office · PDF 合并/拆分（修复占位缺口 + 文档纠偏）

第 18 轮「继续工作」——Trust-but-Verify 发现 PDF 模块是实质 stub：FEATURES.md 曾称「批注/合并/拆分/表单/签名 已实现」，但 `app/js/modules/pdf.js` 中合并/拆分仅为「占位」toast，且无批注/表单/签名代码；只有 pdf.js 离线阅读器可用（vendored `app/vendor/pdf.min.js` 已离线加载，非 CDN）。

### 交付物
- `app/js/pdf-tool.js`（新建，零依赖字节级）：`parsePdf`（对象/Root/页面树解析）、`mergePdfs`（重编号 + 引用重映射 + 重建 Pages/Catalog + 重建 xref/trailer）、`splitPdf`（按 1 基页位置范围抽取）。纯函数 Uint8Array 输入输出，浏览器 + node 双导出。仅支持 PDF 1.4 风格内联对象（含 stream），不支持 object-stream/xref-stream。
- `app/index.html`：在 `js/modules/pdf.js` 前载入 `js/pdf-tool.js`。
- `app/js/modules/pdf.js`：合并按钮→多文件选择→`mergePdfs`→新开 pdf 文档；拆分按钮→页范围输入（如 1-2,3-5）→`splitPdf`→逐份新开文档；删除占位 toast 与误导文案。
- `FEATURES.md`：PDF 行改为诚实标注（阅读✅、合并/拆分✅、批注/表单/签名/PDF⇄Office ❌）；已知事项补 PDF 合并/拆分限制。

### 验证
- `_pdf_tool_test.js` 17/17（2 页 + 3 页 fixture；merge→5 页且 B 字体引用重映射为 13 0 R；split [1,2]→2 页、逐页→各 1 页；合并后再拆分端到端；空范围不崩）。
- `_app_boot_test.js` 升级至 13/13（新增 A7 断言 `OS.PdfTool` 已注册）。
- 全量 **33 套件 0 失败**（上轮 32，+1）。
- 改动文件 `node --check` 全 OK。

### 假设与风险
- 假设「常见 PDF 1.4 内联对象」覆盖绝大多数办公导出/扫描件；PDF 1.5+ 压缩结构（object-stream/xref-stream）会失败并 toast 提示（已文档化，非阻断）。
- 风险：合并/拆分输出未经真实 pdf.js 渲染回归（沙箱无 canvas/pdf.js 运行环境）；结构正确性已由自身 parser 重解析 + xref 偏移自洽验证。
- 风险：依赖 `OS.shell.openDoc` 在新文档打开后由 pdf 模块经 pdf.js 渲染——逻辑与现有阅读路径一致。

## 绿角犀 Office 第19轮（继续工作）
- 核实：核心编辑/四端同源/发版回退/云端后端/PDF合并拆分均已实跑验证，无未闭环代码缺口。
- PDF 导出经 window.print()（浏览器/Electron 打印到 PDF），可用，非坏链，未改。
- 新增 scripts/run-tests.js：零依赖统一测试运行器，自动发现 _*_test.js，子进程隔离+超时，退出码非零失败。
- 新增 _run_tests_test.js（9断言）做运行器自身回归；package.json 增 test / test:runner。
- 新增 .github/workflows/tests.yml：push/PR 到 main 跑 npm test，失败阻断合并。
- 修正 auth.js 过时注释（云端后端未实现→已落地 server/index.js）。
- 基线：34 套件全部 PASS（0 失败），含云后端集成 ~19-23s。Definition of Done 的测试门禁已补齐。

## 绿角犀 Office 第20轮（继续工作）
- 选下一个占位缺口：AI 云端此前为「浏览器直连自有 endpoint + 存本机 Key」占位，FEATURES 标 🟡。
- 新增服务端 AI 流式代理 server/index.js：POST /api/ai/chat（鉴权后）透传上游 OpenAI 风格 SSE，真实 Key 经 LVJX_AI_* 仅存服务端不下发前端；未配置回 501 ai-not-configured；/api/health 加 aiProxy 标志；start(0) 修为随机端口（测试用）。
- 前端 ai.js：setProxyProvider/isProxy，代理模式附登录态 Bearer token，501 静默回退本地；shell.js AI 面板加「使用服务端 AI 代理」开关（与手动 endpoint 二选一）。
- 新增 _server_ai_proxy_test.js（12 断言）：起 mock 上游 SSE，验证 health 标志/401/200 透传/服务端密钥隔离/上游 5xx 透传/501。数据目录临时隔离避免 409。
- 修复既有 _auth_backup_test.js 抖动：确定性 stub window.fetch 拒绝，消除 Node 全局 fetch 行为差异导致的偶发失败（属真实确定性改进）。
- FEATURES.md AI 行 🟡→✅；CLOUD.md 补第八节 AI 代理部署。
- 基线：35 套件全部 PASS（0 失败），三次跑稳定。

## 绿角犀 Office 第21轮（用户指令：本地和云端同用一个帐号就行）
- 落实用户决策：合并「本地账户/云端账户」两套 provider 为单一账号。
- server/index.js：登录路由区分 404（用户不存在）与 401（密码错误），便于前端自动补注册统一两端。
- auth.js 重构：移除 provider(local/cloud) 分裂，统一 register/login（永远先本地离线校验，再尽力连云端取 token；404 自动补注册、401/不可达静默降级）；session={username,token?}；新增 isCloudLinked()/ensureLocal/linkCloud。
- cloudsync.js：shouldSync 改为仅依赖 session.token（不再看 provider）。
- shell.js + index.html：登录页合并两个 Tab 为单一表单；onLogin/onRegister 替代 onLocal*/onCloud*；账户面板按 token 判定云端态。
- 测试：_auth_backup_test 改统一 register/login（修「密码错误」断言用 6+ 位错误密码 wrongpw，原 wrong 被强度校验提前拦截）；_cloudsync_test/_cloudsync_integration_test 去 provider；新增 _unified_account_test.js（覆盖离线注册/缺失 404 补注册/401 降级）；删 _dbg_sync.js。
- 文档：FEATURES.md 账户行改「统一账号」；CLOUD.md 第三/七节去 provider/loginCloud 过时表述。
- 基线：36 套件全部 PASS（0 失败）。

## 绿角犀 Office 第22轮（继续工作）
- 实查发现客户端 webroot 漂移：iOS/HarmonyOS 缺 js/cloudsync.js、js/pdf-tool.js，且 index.html/ai.js/auth.js/modules/pdf.js/shell.js/store.js 共 6 文件停留旧版（双 Tab 登录页、缺云端同步与 PDF 合并拆分）。原因：第16~21轮改 app/ 后未跑 client:sync。
- 执行 node scripts/sync-clients.js 复制 8 文件到两端；--check 现 0 缺失 0 漂移（已同源 ✅）。vendor/ 经核实早已存在客户端、无漂移（pdf.js 为本地内置非 CDN，符合离线可用）。
- package.json 的 test 改为 `node scripts/run-tests.js && npm run sync:check`，把客户端同源检查纳入本地测试门禁（此前仅 CI client-sync.yml 门禁），防「改 app 忘同步」。
- FEATURES.md 顶部过时数字 35→36。
- 基线：npm test 整链 36 单测绿 + 客户端同源绿（exit 0）。

## 绿角犀 Office 第23轮（继续工作）
- 选 PDF 模块首个 🔴 缺口落地：PDF 批注（高亮/画笔/文字批注/矩形），表单/签名/PDF⇄Office 仍规划中。
- 新增 app/js/modules/pdf-anno.js（新建，零依赖纯模型，不依赖 pdf.js）：OS.PdfAnno 提供 add/remove/update/getById/getPage/stats/clearPage、toJSON/fromJSON（容错空/坏串/已数组）、normRect/denormRect（像素↔0..1 归一化，与缩放解耦）、polyBBox（画笔包围盒）、newId（唯一）、COLORS 常量。坐标统一归一化到页面盒子。
- app/js/modules/pdf.js 重写接入：每页渲染 `.pdf-ovl` 覆盖层（pointer-events 随工具切换）；批注工具组（选择/高亮/画笔/文字批注/矩形 + 删除选中/清空），指针绘制→归一化→存入 doc.data.annotations 随文档 serialize 持久化；select 模式点选批注可删；文字批注走 prompt 输入。
- app/index.html：在 pdf.js 后载入 pdf-anno.js。
- 新增 _pdf_anno_test.js（31 断言）：增删改查/按页过滤/统计/JSON 往返/几何归一化往返/包围盒/空坏串容错/newId 唯一。
- FEATURES.md：PDF 行补「批注已实现」；顶部 36→37 套件；已知事项标注批注为覆盖层持久化（非写入 PDF 文件流）。
- 同步：node scripts/sync-clients.js 复制 3 文件（pdf-anno.js 新增 + index.html/pdf.js 漂移）到 iOS/HarmonyOS webroot，--check 现 0 缺失 0 漂移（已同源 ✅）。
- 基线：npm test 整链 **37 套件 0 失败**（上轮 36，+1）；app 启动测试仍 PASS，证明 pdf.js 重写无回归。

### 假设与风险
- 批注为「覆盖层 + 文档内持久化」方案，**不写入 PDF 文件字节流**：换设备需经云端账户/`.lvjx` 携带 annotations 字段；导出的 PDF 文件本身不含批注（属原型可接受，非阻断）。
- 风险：覆盖层渲染需在真实 pdf.js 运行环境目检（沙箱无 canvas/pdf.js）；模型逻辑已由 _pdf_anno_test 全绿覆盖，DOM 集成路径与现有 renderAll 一致。
- 剩余 🔴：PDF 表单 / 签名 / PDF⇄Office 转换（大型，独立排期）。

## 绿角犀 Office 第24轮（继续工作）
- 承接上轮 PDF 批注：让批注可**扁平化导出为真实 PDF**（此前批注仅存应用内，无法分享）。
- app/js/pdf-tool.js 新增 `writeImagePdf(pages)`（async，纯逻辑）：每页 RGBA→DeviceRGB+FlateDecode 图像，构建 Catalog/Pages/Page/Image XObject/Content，复用现有 assemble；FlateDecode 用 CompressionStream（浏览器/Node 22 原生，zlib 格式符合要求）。另导出 `_deflate`/`_inflate`/`_concat` 供测试。
- app/js/modules/pdf.js：批注「管理」组新增「导出带批注PDF」按钮；`exportAnnotated()` 逐页将 pdf.js 渲染画布 + 批注覆盖层绘制到离屏画布（高亮 alpha 填充、矩形描边、画笔折线、文字批注圆点），取 RGBA 调 writeImagePdf 生成 `<名>_批注.pdf` 下载；无 pdf.js 时提示。
- 新增 _pdf_image_test.js（14 断言，异步）：多页生成、parsePdf 页数=3/Root、3 个 Image XObject、MediaBox 尺寸一致、startxref 偏移指向 xref 表、二次生成、_deflate/_inflate 像素往返（含 2000 字节随机块）。
- FEATURES.md：PDF 行补「可扁平化为带批注 PDF」；顶部 37→38 套件；已知事项注明批注可导出扁平化 PDF。
- 同步：node scripts/sync-clients.js 复制 2 文件（pdf-tool.js/pdf.js 漂移）到 iOS/HarmonyOS；--check 已同源 ✅。
- 基线：npm test 整链 **38 套件 0 失败**（上轮 37，+1）；app 启动测试仍 PASS。

### 假设与风险
- writeImagePdf 为**图像型 PDF**（页面栅格化），导出后文字不可选、体积较原始文本 PDF 大（FlateDecode 整体压缩）；属批注分享的合理权衡，非阻断。
- 导出还原依赖真实 pdf.js 渲染（沙箱无 canvas），DOM 扁平化绘制逻辑未单测；核心 writeImagePdf 已由 14 断言全绿覆盖，集成路径与现有 renderAll 一致。
- 剩余 🔴：PDF 表单 / 签名 / PDF⇄Office 转换。

## 绿角犀 Office 第25轮（继续工作）
- 承接上轮：落地独立 **签名（sign）** 能力，区别于自由画笔——手写笔画固化为可复用印章盖到页面。
- app/js/modules/pdf-anno.js：新增 `sign` 类型与 `COLORS.sign`(#1c1b18 墨黑)；`add()` 处理 `strokes`（笔画数组，归一化至印章自身 0..1 盒子）+ 放置 `x,y,w,h`；`stats.byType` 增 `sign`。
- app/js/modules/pdf.js：批注「工具」组新增「签名」按钮（icon:edit，已在 icons.js 补 pencil 图标）；`renderAnnotations` 绘 sign 笔画（SVG polyline，按放置缩放）；`exportAnnotated` 扁平化导出 sign；新增 `openSignPad()` 手写板弹窗（canvas 采集笔画→归一化）+ `stampSignature()`（选距视口中心最近页盖章，按笔画宽高比定印章尺寸并夹取页面内）。
- icons.js 一并补齐既有债务：原 pen/highlight/cursor/note/rect 未在 PATHS，导致批注工具栏按钮仅渲染标签无图标；本轮补 edit/pen/cursor/note/rect/highlight 六个图标，批注工具栏现全部带图标。
- 新增 _pdf_sign_test.js（17 断言）：sign 落库/笔画+放置/自定义墨色/缺笔画容错/getPage/stats.byType.sign/JSON 往返/坏串容错。
- app/js/icons.js：PATHS 加 `edit` pencil 图标。
- FEATURES.md：PDF 行补「签名印章」；顶部 38→39 套件；已知事项注明签名实现。
- 同步：node scripts/sync-clients.js 复制 3 文件（pdf-anno.js/pdf.js/icons.js）到 iOS/HarmonyOS；--check 已同源 ✅。
- 基线：npm test 整链 **39 套件 0 失败**（上轮 38，+1）；app 启动测试 PASS。

### 假设与风险
- 签名为「覆盖层 + 文档内持久化」，不写入 PDF 文件字节流（与批注一致）；导出带批注 PDF 已含签名栅格。
- 手写板/盖章 UI 依赖真实 DOM/canvas（沙箱无），未单测；模型与导出写入逻辑已由 17+14 断言覆盖，集成路径与现有 renderAnnotations/renderAll 一致。
- 剩余 🔴：PDF 表单 / PDF⇄Office 转换（大型，独立排期）。

## 绿角犀 Office 第26轮（继续工作）
- 承接上轮：落地 **PDF 文本层 + 全文搜索**（与批注/签名正交，补齐阅读器核心能力）。
- 新增 app/js/modules/pdf-text.js：`OS.PdfText` 纯逻辑——`buildIndex(pages)`（每页归一化 span 数组）/ `search(index,query)`（大小写不敏感、跨页子串）/ `hitPages` / `pageText` / `allText`；坐标夹取与脏数据容错。不依赖 pdf.js，node 直跑。
- index.html：在 js/modules/pdf.js 之前引入 pdf-text.js（零构建加载顺序）。
- app/js/modules/pdf.js：顶部加 `const PdfText = OS.PdfText`；`renderAll` 每页 `await page.getTextContent()` → 映射归一化 span → 构建 `.pdf-text-layer`（透明可选中 span，置于 canvas 之上、批注 overlay 之下）→ 存 `textIndex.pages[p-1]`；view 顶部加搜索条（`.pdf-search` input + 计数），`runSearch(q)` 高亮命中 span（黄底）并滚动首个匹配；`exportAnnotated` 不受影响（文字已在 pdf.js 渲染中，导出图像含文字）。
- 新增 _pdf_text_test.js（28 断言）：buildIndex 存储/脏数据容错/坐标夹取、search 大小写不敏感/跨页/无匹配/空 query/坏 index、hitPages、pageText/allText。
- FEATURES.md：PDF 行补「文本层/搜索（可选中复制+全文搜索高亮）」；顶部 39→40 套件；已知事项注明文本层为覆盖层方案、纯逻辑可单测。
- 同步：node scripts/sync-clients.js 复制 3 文件（pdf-text.js/pdf.js/index.html）到 iOS/HarmonyOS；--check 已同源 ✅。
- 基线：npm test 整链 **40 套件 0 失败**（上轮 39，+1）；app 启动测试 PASS。

### 假设与风险
- 文本层为 **pdf.js 文本项映射的透明可选中覆盖层**（非 OCR）：依赖真实 pdf.js getTextContent/Util.transform（沙箱无），DOM 文本层与搜索高亮未单测；`OS.PdfText` 纯逻辑已由 28 断言全绿覆盖，集成路径与现有 renderAll 一致。
- 透明文字选中后选区高亮可见、可复制；搜索命中黄底定位。导出 PDF 已是图像型，文字随页面渲染包含。
- 剩余 🔴：PDF 表单 / PDF⇄Office 转换（大型，独立排期）。

## 绿角犀 Office 第27轮（继续工作）
- 承接上轮：落地 **PDF 表单填写**（文本框 + 复选框）——PRD 明确并列、与批注/签名/文本层正交、可零依赖覆盖层方案闭环的最后一个轻量 PDF 能力。
- app/js/modules/pdf-anno.js：`add()` 新增 `textfield`（归一化矩形 x/y/w/h + 可编辑 `text` 兜底空串）/ `checkbox`（见方 + `checked` 经 `!!` 收敛）分支；`COLORS` 加 `textfield:#1565c0` / `checkbox:#2e7d32`；`stats.byType` 加 `textfield`/`checkbox`。
- app/js/icons.js：PATHS 加 `textfield`（输入框图标）/ `checkbox`（方框+✓ 图标）。
- app/js/modules/pdf.js：`onPointerDown` 新增 checkbox 分支（点击放置 0.03 见方、checked:false）；`onPointerUp` 的拖拽块内新增 textfield 分支（prompt 输入文字 → 落库）；`renderAnnotations` 新增 textfield（白底边框 div+文字）与 checkbox（边框方框 + ✓ 当 checked）渲染；`decorate` 重写分支——checkbox 单击切换 checked 重渲染、textfield 双击编辑/单击选中、其余保持原行为；工具栏签名后新增「文本框」「复选框」按钮；`exportAnnotated()` 扁平化导出新增 textfield（白底+边框+文字）与 checkbox（见方+✓）分支，闭环「导出带批注 PDF」含表单。
- 新增 _pdf_field_test.js（24 断言）：textfield/checkbox 落库、文字/checked 存储、缺省兜底、checked 脏值容错、update、getPage、stats.byType、JSON 往返含文字/checked、坏串容错、clearPage。（初版 total/checkbox 断言笔误 6/4 → 修正为 5/3，重跑全绿。）
- FEATURES.md：PDF 行补「表单填写已实现」（文本框/复选框随文档持久化+参与导出扁平化）；顶部 40→41 套件；已知事项注明表单已可用、仅 PDF⇄Office 仍 🔴。
- 同步：node scripts/sync-clients.js 复制 3 文件（pdf-anno.js/pdf.js/icons.js）到 iOS/HarmonyOS；--check 已同源 ✅。
- 基线：npm test 整链 **41 套件 0 失败**（上轮 40，+1）；app 启动测试 PASS。注：全量并行跑偶发 `_auth_backup_test` 失败一次（51 断言），隔离重跑与二次全量均稳定通过，与本轮 PDF 改动无关（属并行子进程资源争用抖动）。

### 假设与风险
- 表单字段为「覆盖层 + 文档内持久化」，与批注/签名一致，**不写入 PDF 文件字节流**；导出带批注 PDF（图像型）已含表单栅格。换设备需经云端账户/`.lvjx` 携带 annotations。
- 文本框双击编辑/复选框单击切换为真实 DOM 交互（沙箱无），未单测；模型与导出绘制逻辑已由 24 断言覆盖，集成路径与现有 renderAnnotations/exportAnnotated 一致。
- 剩余 🔴：PDF⇄Office 转换（大型，独立排期，零依赖 sandbox 内不可行，本轮跳过）。

## 绿角犀 Office 第28轮（继续工作）
- 评估现状：FEATURES 仅剩 🔴 为 PDF⇄Office 转换（大型、沙箱不可行）；但代码层发现真实缺口——`export-ooxml.js:224` 的 DOCX 导出对 `<img>` 是占位空 run，Writer 文档图片导出到 DOCX 被静默丢弃（已标 ✅ 的 OOXML 导出潜在数据缺失）。
- 确认前置：Writer 插入图片走 `OS.util.readFile(f,true)` → `readAsDataURL`，存储为 `data:` URI，故 data-URI 嵌入即真实路径。
- 落地 DOCX 图片嵌入（`app/js/export-ooxml.js` `buildDocx`）：`addImage(src)` 仅支持 `data:` URI（base64 解码→字节，按 mime 定扩展名/内容类型，解析 PNG/JPEG/GIF 内嵌尺寸→EMU，非 data URI 优雅跳过）；`drawingRun()` 生成 `wp:inline`+`pic:pic` drawing run；document.xml 根补 `wp`/`pic` 命名空间；`document.xml.rels` 加 image 关系；`[Content_Types].xml` 加对应 Default 类型；`word/media/imageN.*` 写入（用 `zip.file(name, b64, {base64:true})` 规避 jsdom realm 的 Uint8Array 跨域不被 Node 版 JSZip 识别的问题）。
- `_exporter_test.js`：JSZip 改由真实 npm 模块注入（`window.JSZip = require('jszip')`，jsdom 注入版 async 会挂起）；新增 6 断言——含 1x1 PNG data URI 的文档导出后 `word/media/image1.png` 存在、document.xml 含 `<w:drawing>` 与 `r:embed="rId50"`、rels 含 image 关系、[Content_Types] 含 png 默认类型；外部 `https://` 图片 src 优雅跳过无 media。
- FEATURES.md OOXML 导出行注明 DOCX 现含图片嵌入（不再静默丢弃）。
- 同步：node scripts/sync-clients.js 复制 1 文件（export-ooxml.js）到 iOS/HarmonyOS；--check 已同源 ✅。
- 基线：npm test 整链 **41 套件 0 失败**（套件数未变，_exporter_test 断言 11→17）；app 启动测试 PASS。

### 假设与风险
- 仅支持应用内 base64 内联图片（data: URI）；`blob:`/`http(s)` 外部引用因同步构建期无法取字节而优雅跳过（留空 run，不崩）。Writer 实际存储即 data URI，覆盖主路径；外部引用属边缘场景，非阻断。
- 图片尺寸取自字节内嵌宽高（PNG/JPEG/GIF 解析；失败回退 600×400px @9525 EMU/px）。导出为图像型嵌入，Word/WPS 可正常显示。
- 测试依赖 JSZip 3.0 无 `asText()` 且 jsdom 注入版 `async()` 挂起，故改用真实 npm JSZip（DOMParser 仍来自 jsdom），`.async("string")` 在纯 Node 下 1ms 解析；此改动不影响被测产物（仅换 JSZip 来源）。

## 绿角犀 Office 第29轮（继续工作）
- 评估现状：FEATURES 仅剩 🔴 为 PDF⇄Office 转换（大型、沙箱不可行）；上轮已补 DOCX 图片**导出**，故巡检 OOXML 图片往返——发现 `import-ooxml.js` 的 `parseDocx` 完全不处理图片（`runsToHtml`/`runToHtml` 只解析文本/批注/超链接，遇到 `<w:drawing>` 静默丢弃），导致「导出带图 DOCX → 导入再编辑」往返丢图，是同类数据缺失。注：Spreadsheet/Presentation 模块本身不支持用户插入图片，故 XLSX/PPTX 用户图片嵌入非当前缺口（用户插不了图）。
- 落地 DOCX 图片**导入读回**（`app/js/import-ooxml.js`）：模块级新增 `IMG_EMBED` 缓存 + `mimeFromPath` + `drawingToImg`（查缓存生成 `<img>`）+ `prefetchDocxImages`（parseDocx 入口预扫描 document.xml 全部 `a:blip r:embed`，`Promise.all` 批量异步读 `word/media/*` 字节转 data URI 入缓存）；`runToHtml` 新增 `drawing` 分支——收集到 `imgHtml` 并在 `esc` 之后的 `s` 上拼接（避免被 `esc()` 转义破坏标签）；暴露 `parseDocx` 到 `OS.Importer`。
- 新增 `_importer_test.js`（14 断言）：复用 jsdom+npm JSZip harness，先用 `OS.Exporter.buildDocx` 生成含 1x1 PNG data URI 的 DOCX（npm JSZip 实例）→ `OS.Importer.parseDocx` 解析 → 断言 `res.data.html` 含 `<img src="data:image/png;base64,...">`、含原始 base64、未被转义（`&lt;img` 不应出现）；无图文档不产生 `<img>`；外部 `https://` 图片文档不崩、不产生 data:image。形成导出+导入图片闭环。
- FEATURES.md：OOXML 导入行注明 DOCX 现可往返图片；顶部 41→42 套件。
- 同步：node scripts/sync-clients.js 复制 1 文件（import-ooxml.js）到 iOS/HarmonyOS；--check 已同源 ✅。
- 基线：npm test 整链 **42 套件 0 失败**（上轮 41，+1）；app 启动测试 PASS。

### 假设与风险
- 图片读回为「导出带图 DOCX → 导入为 Writer `<img>` data URI」的往返闭环；仅 `data:` URI 内联图片可还原（外部链接因无字节而跳过，且 Export 阶段本就不生成其 drawing，导入自然无图）。
- `IMG_EMBED` 为模块级变量，parseDocx 入口重置；单线程串行解析无竞态，并发解析理论上可能串扰（本项目 importFile 串行，风险低，非阻断）。
- 剩余 🔴：PDF⇄Office 转换（大型，独立排期）。

## 第 30 轮「继续工作」——PDF→DOCX 文本提取（把唯一 🔴 推到 🟡）

- 评估现状：FEATURES 唯一 🔴 仍是 PDF⇄Office 转换（大型、沙箱不可行）。但 OOXML 图片已闭环（上两轮补了 DOCX 导出+导入图片）；Spreadsheet/Presentation 不支持插图片，非缺口。确定切入点：复用 `textIndex.pages` 已提取的归一化文本坐标（y 页顶向下、h 为字号），做 **PDF→DOCX 文本提取骨架**（纯逻辑可单测、可交付）。
- 新增 `app/js/modules/pdf-convert.js`（`OS.PdfConvert`）：`clusterLines(pages)` 按 y 容差（max(0.45h,0.01)）聚类成行、行内按 x 升序；`pdfToDocxHtml(pages)` 大字号（>0.022）判 `<h2>` 标题、其余 `<p>`、跨页插 `<div class="page-break">`；`pdfToDocx(pages,name)` 调 `OS.Exporter.buildDocx` 生成 zip。保留 `module.exports` 供 node 单测。
- `app/js/icons.js` 新增 `docx` 图标；`app/index.html` 在 `export-ooxml.js` 后注入 `pdf-convert.js`；`app/js/modules/pdf.js` 批注工具栏导出区新增「导出为Word」按钮 + `exportAsWord()`（校验 pdfDoc/textIndex → `OS.PdfConvert.pdfToDocx` → generateAsync blob → `OS.util.download` 下载 `.docx` → toast 提示「文本提取、版面还原有限」）。
- 新增 `_pdf_convert_test.js`（19 断言）：复用 jsdom+npm JSZip harness，构造含坐标/字号假 pages，断言 clusterLines 行序与行内排序、pdfToDocxHtml 含 `<h2>`/`<p>`、闭环 pdfToDocx→zip.word/document.xml 含提取文本与标题；坏数据容错（null/[]/非数组）。
- 文档修复：`app/README.md` 第 27 行「合并/拆分为占位入口」过时——改为列举 PDF 已实现能力（阅读/合并拆分/批注/文本层搜索/表单/文本提取 DOCX）；`FEATURES.md` PDF⇄Office 由 🔴 改 🟡（PDF→DOCX 已实现、版面还原有限；PDF→Excel/docx→PDF 仍规划中），顶部 42→43 套件。
- 同步：node scripts/sync-clients.js 复制 5 文件到 iOS/HarmonyOS；--check 已同源 ✅。
- 基线：npm test 整链 **43 套件 0 失败**（上轮 42，+1）；app 启动测试 PASS。

### 假设与风险
- PDF→DOCX 仅文本层提取（按坐标聚类成行、大字号判标题），**无图片/表格/精确坐标/多栏还原**；纯文本型 PDF（如报告、论文）效果最佳，扫描件/复杂版面还原有限（已通过 toast 诚实告知用户）。
- 这是唯一 🔴 的「可交付第一步」，把状态从 🔴 规划中推到 🟡 部分实现；剩余 PDF→Excel、docx→PDF 仍属大型能力（需表格结构识别/版式重排），独立排期。
