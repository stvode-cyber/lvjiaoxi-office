# 2026-08-15 工作日志

## 登录系统 + 50M 个人空间 + 每 5 分钟自动备档
- 新增 `app/js/auth.js`：本地账户（SHA-256+salt，localStorage）、云端账户接口骨架（API_BASE=/api + /auth/*，fetch 8s 超时，后端未部署时优雅降级）、会话（sessionStorage，会话级）、常量 `QUOTA=50MB` / `MAX_BACKUPS=20` / `BACKUP_INTERVAL=5min`、按账户隔离的 IndexedDB 库名。
- 改造 `app/js/store.js`：`init` 按账户隔离库（lvjiaoxi-office-<user>）、新增 `backups` store（docId 索引）；新增 `backup/backupAll/listBackups/listAllBackups/getBackup/removeBackup/pruneBackups/restoreBackup/spaceUsed/quota/withinQuota`；备档 size 计入个人空间；内存降级模式同样支持。
- 登录 UI：`index.html` 加 `#login-overlay`（本地注册/登录 + 云端登录 双 tab）；`shell.js` 的 `boot` 加登录拦截（未登录显登录页）；`enterApp` 初始化账户隔离 store + 启动备档定时器 + 渲染 dashboard；账户抽屉增强（账户名/provider、50M 空间进度条、备档列表+回滚、立即备档、退出登录）；`style.css` 加登录页/空间条/备档列表样式。
- 回归：新增 `_auth_backup_test.js`（27 断言，jsdom 加载 auth+store 走内存降级）。全量回归 16 套件 **347 passed / 0 failed**（原 305 不破坏）。
- 关键修复：`auth.js` 会话改用独立 `_session` 变量，避免覆盖 `current` 方法；云端网络错误判定兼容跨 realm（`e.name === "TypeError"`）。
- 注意：云端账户后端为骨架预留，本版本未部署服务端，云端登录将提示「未接入」；真实隔离在 IndexedDB 模式生效（内存降级模式共享单例）。

## 「记住我」持久会话 + 子系统文档
- `auth.js`：`setSession(s, remember)` 支持 `remember` 参数，勾选时将会话写入 `localStorage`（key `lvjiaoxi-remember`），否则清除；`current()` 优先级 sessionStorage → localStorage（记住我跨会话恢复）→ 内存；`logout()` 同时清除两者。`registerLocal/loginLocal/registerCloud/loginCloud` 均透传 `remember`。
- `index.html`：本地/云端登录表单各加「记住我」复选框（`#remember-me` / `#remember-me-cloud`）。
- `shell.js`：四个登录/注册入口从对应复选框读取 `remember` 传入。
- `style.css`：补 `.remember-row` / `.remember-label` 样式。
- 文档：新增 `ACCOUNT_BACKUP.md`（登录/空间/备档/记住我 子系统说明 + 常量表 + 安全说明）。
- 回归：`_auth_backup_test.js` 扩至 **35 断言**（新增 8 个记住我：写入/跨会话恢复/未勾选不持久/登出清除）；全量回归 **355 passed / 0 failed**。
- 注：`boot()` 已能从 `localStorage` 自动恢复记住我会话免登录（current() 自然支持）。

## 个人空间导出 / 导入（迁移 · 外部备份）
- `store.js` 新增 `exportSpace()`（打包当前账户 docs+backups 为 `{format:"lvjx-space",version:1,account,docs[],backups[]}`）与 `importSpace(data,{replace})`（校验格式、按 id 合并文档与备档，replace 先清空再导入；损坏包抛「文件格式不支持或已损坏」）。
- `shell.js` 账户抽屉加「空间迁移 / 外部备份」小节：「导出空间」(downloadSpace 用 Blob+a[download] 下 `.lvjx`，文件名含用户名+日期)、「导入空间」(隐藏 file input → 读 JSON → importSpace → 刷新空间/备档并 emit docs-changed)；新增 `downloadSpace` 辅助函数（无第三方依赖）。
- 文档 `ACCOUNT_BACKUP.md` 增第 5 章「空间导出 / 导入」。
- 回归：`_auth_backup_test.js` 扩至 **42 断言**（新增 7 个导出/导入）；全量回归 **362 passed / 0 failed**。
- 用途：换机迁移、本机 IndexedDB 丢失的外部备份、跨设备合并。

## 采纳优化建议（全量采纳 + 纠正一处误判）
- **重要纠正**：此前功能清单误判「OOXML 双向保真 / OFD 导入」未实现。回查确认 `app/js/export-ooxml.js`(`OS.Exporter`：writer→DOCX / spreadsheet→XLSX / presentation→PPTX / writer+presentation→OFD) 与 `app/js/import-ooxml.js`(`OS.Importer`：docx/xlsx/pptx/odt/ods/odp/ofd 往返导入) **早已完整实现**，`shell.js` 统一导出已路由原生格式；模块级 `exportAs` 的 docx 分支是遗留死代码。**唯一真正未实现的是 PDF⇄Office 转换**。
- 清理：`writer.js` 的 `exportAs` docx 分支改路由 `OS.Exporter`（移除对未引入的 `htmlDocx` 依赖，该分支此前静默失败）；删除误下载的 `app/vendor/html-docx.js`(45B 损坏) 与未使用的 `app/vendor/xlsx.full.min.js`(880KB)，保持离线包精简。
- `.lvjx 加密`：`store.js` 加 `encryptEnvelope/decryptEnvelope`（PBKDF2-SHA256 150k 迭代 + AES-GCM-256），`exportSpace({passphrase})`/`importSpace(data,{passphrase})` 支持可选口令；`shell.js` 账户抽屉加「导出加密口令」输入，导入加密文件时 `prompt` 索入口令，口令错误明确报错。
- 账户抽屉：`store.js` 加 `docBytes()/backupBytes()`；`renderSpace` 分别展示「文档占用 / 备档占用」并带 >80% 黄、>95% 红告警；登录页加「请勿复用重要在线账户密码」提示（index.html + style.css `.login-pw-warn`）。
- Electron：`electron/main.js` 由固定 8123 改随机空闲端口（`server.listen(0)` 后取 `address().port`），避免被占用/探测。
- 文档：新增 `FEATURES.md`（按 已实现/部分/规划中 三档标注，更正 OOXML/OFD 与 PDF⇄Office 表述）；`CLIENTS_BUILD.md` 标注 iOS/华为为源码工程（本机无 Mac/DevEco）、Android 待生成 AAB；`ACCOUNT_BACKUP.md` 增 5.1 导出加密。
- ESM 迁移评估：暂缓（零构建全局 OS.* 迁移会冲击现有回归，记入 FEATURES.md §六）。
- 回归：新增 `_exporter_test.js`（11 断言，验证原生 DOCX/XLSX/PPTX/OFD 包结构）；`_auth_backup_test.js` 扩至 **49 断言**（新增 8 个 .lvjx 加密：encrypted 标记/无口令拒/错口令拒/正确解密恢复）；全量回归 **17 套件 380 passed / 0 failed**。
- 测试坑：JSZip 的 `async()/generateAsync()` 在 jsdom 下会挂起（浏览器正常），故导出测试仅做部件文件名结构断言，内容往返留待浏览器验证。
