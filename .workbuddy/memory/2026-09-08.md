# 2026-09-08 工作日志

## 完成项
- **修复测试环境（项目迁移 D 盘后的遗留断链）**：
  - `jsdom` / `jszip` 原挂在外部路径 `C:/Users/Administrator/.workbuddy/binaries/node/workspace/node_modules`，随环境迁移丢失 → 改为项目 devDependency（`npm install --save-dev jsdom jszip`）
  - 36 个测试文件 APP 路径 `C:/Users/Administrator/Desktop/绿角犀办公软件` → `D:/源码存档/绿角犀办公软件`
  - `_e2e_roundtrip_test.js` 发布断言 + `_profile_biz_test.js` 版本断言改为从 package.json 动态读取（版本单一真源，避免升版漂移）
  - `.gitignore` 补 `.codegraph/` IDE 缓存
- **全量门禁**：`npm test` **99 套件 0 失败** + 四端同源 ✅（基线从 92 套件 91 通过升至 99 套件 0 失败）
- **文档回写 v1.0.17**（上轮 09-04 升版后未回写）：
  - CHANGELOG：v1.0.17 已发版明细（顶部导航五板块/订单/库存/审批/我的/CSV 导出）+ v1.0.16 归并 AP/AQ/AR + ObjStm
  - STATUS：模块表加业务板块行、基线更新为 99 套件、最后更新 2026-09-08
  - MEMORY：知识库入口指向 handoff-2026-09-08、当前基线更新
  - FEATURES：新增「七、业务板块」章节 + 头部标注更新
- **提交**：`586b6e0 chore(test): 修复测试环境（项目迁 D 盘后 jsdom/jszip 外部路径失效）`

## 测试基线
- **99 套件，0 失败**（v1.0.17 门禁）+ 四端同源 ✅

## 未完成（需用户侧）
- 发布 v1.0.17 到 VPS（`publish-latest.bat`，沙箱网络对 VPS 物理不可达）
- iOS/HarmonyOS 编译上架

## 决策
- 测试依赖本地化：jsdom/jszip 纳入 devDependencies，杜绝对外部 workspace 路径的隐性依赖
- 版本断言动态化：E2E/业务测试读 package.json 版本，配合版本单一真源纪律
