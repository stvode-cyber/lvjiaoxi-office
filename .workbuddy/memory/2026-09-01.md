# 2026-09-01 工作日志

## AN=PDF 字体信息提取（OS.PdfFonts）· 全链路交付

**背景**：延续字母序全权开发，AM 已完成（页面属性/页面树），本轮推进 AN。
选型理由：AN 是 AM「页面/资源」的直接延续，纯字节解析可在沙箱自证，
且产品价值明确（检测未嵌入字体 → 影响显示/打印/转换保真）。

**交付**
- 新模块 `app/js/modules/pdf-fonts.js`（三件套 IIFE + OS 全局 + module.exports）
  - `parseFonts(bytes)`：遍历页面树收集每页 `/Resources /Font`（页面自身缺失时沿 `/Parent` 链向上继承）
  - 逐字体解析：BaseFont（`ABCDEF+` 子集前缀剥离）/ Subtype(Type0·Type1·TrueType·MMType1·Type3·CIDFontType0/2) /
    Encoding(预定义名·引用取 BaseEncoding·Differences 自定义) / ToUnicode /
    嵌入标志(FontFile·FontFile2·FontFile3) / Flags 九位 / 字符范围与宽度表
  - Type0 经 `/DescendantFonts` 下钻到 CIDFont 的 FontDescriptor 判嵌入
  - 按字体对象号去重，合并使用页；无页面树时兜底全局扫描 `/Type /Font`
- UI：`pdf.js` ribbon 新增「字体信息」组（icon `font-size`）→ `showFonts()` 弹窗
  （摘要 + 逐字体清单 + 逐页映射 + 风险红条 + 导出 MD）
- 测试 `_pdf_fonts_test.js`：**74 断言全过**
  （PDF_A 中文 CID 嵌入+跨页去重 / PDF_B 标准14+TrueType+Type3+Flags / PDF_C 资源继承+编码引用 / PDF_D 无字体负例 / PDF_E 无页面树兜底 / 边界空输入）
- 全量门禁 **85 套件 0 失败**（84→85）
- 四端同源 ✅；bump **1.0.13**；构建 `dist/绿角犀 Office Setup 1.0.13.exe` + 便携版

**踩坑（已沉淀至 MEMORY.md）**
1. **`/Flags` 规范上属于 FontDescriptor，不在字体字典里** —— 初版只查字体字典，Flags 全为 null → 2 条断言失败。
   修复：字体字典优先、FontDescriptor 兜底。诊断方式是打印实际值（`flags = null`）而非改断言。
2. 模块只暴露了 4 个标志位布尔字段（fixedPitch/serif/symbolic/italic），缺 `nonsymbolic` 等 →
   补全为 9 位（script/nonsymbolic/allCap/smallCap/forceBold）。**选择补全模块而非放宽测试断言**。
3. MEMORY.md 编辑踩坑：上一轮已存在 AM 行，本轮以 AL 行为锚点插入 AM+AN 会**产生重复 AM 行**，
   需插入后 grep 回查去重（本次已修正）。

**当前状态**
- git 提交 + tag v1.0.13（无 remote，未 push）
- 字母序进度：S→AN，累计 13 个 tag（v1.0.0~v1.0.13）

**下一站 AO 候选（待拍板）**
1. PDF→DOCX 版面还原（最大短板：当前仅文本提取，非像素级还原）
2. 文档合并/拆分增强（兼容 PDF 1.5+ object stream / xref stream，当前仅 1.4 内联）
3. PDF 可选内容组 OCG 图层（解析 /OCProperties，图层列表与默认可见性）
4. PDF 页面缩略图/导航（需真实 Canvas，沙箱不可自证，优先级低）

## 桌面端应用内静默更新（更新机制修复）· 全链路交付

**背景**：用户明确「后面版本更新不需要再去下载原件，而是提示更新、完成更新动作」。
此前桌面端更新逻辑虽写了 electron-updater 集成，但有三处根因导致永远走「前往下载」手动重装：
1. `electron/feed-config.js` 的 `resolveUpdateProvider` 对非 GitHub 的 `version.json.url`（https://lujax.fun/releases）
   返回 null → 无更新源 → `_autoEnabled` 永 false；
2. `electron-updater` 依赖未装本地（try/catch 置 null）→ 主进程 IPC 实际不可用；
3. 前端 `app/js/updater.js` 在 `notify`/`applyUpdate` 前置 `_autoEnabled` 判断 → 桌面只能「前往下载」。

**交付（三处修正）**
- `electron/feed-config.js`：`resolveUpdateProvider` 新增——`versionJsonUrl` 为非 GitHub 的 http(s) 时直接返回
  `{ provider:"generic", url: normalizeFeedUrl(...) }`，使 `lujax.fun/releases` 默认启用静默通道
- `app/js/updater.js`：去掉 `_autoEnabled` 变量与前置判断；桌面端只要 `isNativeShell() && electronAPI.invoke`
  即走 `downloadAndInstall`（静默下载→安装），按钮统一「立即更新」；`downloadAndInstall` 在 `updater:download`
  回传 ok:false 时兜底打开发布页（结构与主进程 `updater:error` 兜底一致）
- 本地装 `electron-updater@^6.8.9`（devDependencies，原 `^6.3.0` 由 npm 解析到 6.8.9）→ 打包后运行时可用
- 测试：新增 `_feed_config_test.js`（10 断言，验证 generic/github/null 解析）；`_updater_fallback_test.js` DOM2
  改为「桌面走静默通道、download 失败回退打开发布页」新契约；全量 **85 套件 0 失败**
- 四端同源同步 ✅；bump **1.0.14**；重建 Windows 安装包
  `dist/绿角犀 Office Setup 1.0.14.exe`(nsis) + `绿角犀 Office 1.0.14.exe`(portable) + `latest.yml`(指向 1.0.14) + .blockmap
- git 提交 `ac147ae` + tag **v1.0.14**（无 remote，未 push）；dist/ 被 .gitignore 忽略，安装包属本地构建产物

**待办（人工/环境）**
- 把 `dist/` 的 `绿角犀 Office Setup 1.0.14.exe` + `latest.yml` + `.blockmap` 托管到 `https://lujax.fun/releases/`，
  旧版客户端才会真正拉到 latest.yml 走应用内静默更新；否则仍走兜底打开发布页
- git remote 待配置后 `git push --tags`（v1.0.0~v1.0.15）

## 自动更新模式升级（v1.0.15）
- 背景：用户明确要"自动更新模式"，而非 1.0.14 的"提示更新→点立即更新"半自动。
- 改动：
  - electron/main.js：`autoUpdater.autoDownload = true`（启动即 checkForUpdates + 后台静默下载）；autoInstallOnAppQuit=true 保留；setupAutoUpdater 末尾自动 checkForUpdates()。
  - app/js/updater.js：notify 去掉「立即更新」按钮→"后台自动下载中…"；updater:downloaded 改「立即重启 / 稍后」二选一（不点则退出时自动装）。
- 验证：node --check 两文件通过；整链 85 套件 0 失败；sync-clients --check 四端同源 ✅。
- 交付：bump 1.0.15（package.json/index.html/version.json/sw.js CACHE v13→v14/iOS/HarmonyOS）；重建安装包 Setup 1.0.15.exe + 便携版 + latest.yml(.blockmap)；commit 3919fa9 + tag v1.0.15（无 remote 未 push）。
- 待发布（人工/环境）：把 dist/ 的 Setup 1.0.15.exe + latest.yml + .blockmap 托管到 https://lujax.fun/releases/，旧客户端才会真正走应用内自动更新。

## 发布流程优化（publish-desktop-feed --latest-only）
- 背景：dist/ 累积 1.0.5~1.0.15 全部历史安装包，原 `node scripts/publish-desktop-feed.js` 会收集 28 个文件（~2GB）一并上传，浪费带宽。
- 改动：`collectArtifacts(distDir, {latestOnly})` 新增 `--latest-only`：读 `dist/latest.yml` 的 `version`，只保留该版本号匹配的 nsis/便携/blockmap + latest*.yml；`main()` 解析参数，dry-run 显示 latestOnly 状态。
- 验证：`_publish_feed_test.js` 22 断言全过（含 latest-only 只选最新版、排除旧版本）；真实 dist dry-run 仅 4 文件（1.0.15 的 nsis+便携+blockmap+latest.yml）。
- 发布命令（待凭据）：`LVJX_FEED_SSH_HOST/USER/KEY(base64)/REMOTE_DIR` 或 `LVJX_FEED_S3_BUCKET/PREFIX` 设好后，`node scripts/publish-desktop-feed.js --latest-only` 即发布 1.0.15；CI `release.yml` 已接线 secrets/vars 自动发布。

## 发布通道可行性探查（17:58，用户拍板 1.2.3 后）
- 实测：SSH 8.149.245.252:22 → 超时（阿里云安全组白名单，沙箱出口 IP 不在内；用户今天 13:01 是在另一台能连的机器连的）；SSH 47.116.59.141:22 → 能连但 rcprod key 被拒（非 lujax 服务器）；沙箱 HTTPS 到 lujax.fun/releases 无响应；`git remote -v` 为空。
- 结论：1.2.3（发布 / CI 自动发 / git push）三项均依赖外部服务器访问授权或 git remote，**当前沙箱无法独立完成**——须用户在能连 8.149 VPS 的机器跑发布命令，或给 git remote 地址。
- 已就绪（非代码阻塞）：发布脚本 --latest-only dry-run 仅选 1.0.15 四产物（22 断言过）；CI release.yml 已接线 secrets；dist/ 已有 1.0.15 安装包+latest.yml。
- 交付用户的执行清单：阶段1 `ssh -i ~/.ssh/id_ed25519_rcprod root@8.149.245.252 'find /var/www /opt /srv /usr/share/nginx -maxdepth 4 -type d -name releases'` 只读探测目录；阶段2 `LVJX_FEED_SSH_HOST=8.149.245.252 LVJX_FEED_SSH_USER=root LVJX_FEED_SSH_KEY=$(base64 -w0 ~/.ssh/id_ed25519_rcprod) LVJX_FEED_SSH_REMOTE_DIR=<DIR> node scripts/publish-desktop-feed.js --latest-only`（在能连 VPS 的机器、项目目录下执行）。
- 脚本增强（18:35，落实用户 1.2.3 第3点）：`publish-desktop-feed.js` 新增 `resolveRemoteDir`/`pickReleasesDir`——SSH 模式 `LVJX_FEED_REMOTE_DIR` 可省略，发布时自动 `find` 远端第一个 releases 目录（rsync 前定位）；`_publish_feed_test.js` 增 5 断言（pickReleasesDir 解析），全量仍 85 套件 0 失败；commit `31fb1a0`。用户执行收敛为一行：`LVJX_FEED_SSH_HOST=8.149.245.252 LVJX_FEED_SSH_USER=root LVJX_FEED_SSH_KEY=$(base64 -w0 ~/.ssh/id_ed25519_rcprod) node scripts/publish-desktop-feed.js --latest-only`（在能连 VPS 的机器、项目目录跑，无需记目录）。
- 发布封装脚本（19:25，第三次拍板 1.2.3）：新增 `scripts/publish-latest.sh`（纯 ASCII bash，自动 cd 项目根 + 默认 host/user + 自动 base64 私钥 + 自动定位目录），用户本机 Git Bash `bash scripts/publish-latest.sh` 即发布最新版；commit `668db22`。（git push 仍待 remote 地址：用户给地址后 `git remote add origin <URL> && git push --tags` 即推送 v1.0.0~v1.0.15）
- 自动更新正向链路测试（20:44，「继续工作」）：新增 `_updater_autoupdate_test.js`（11 断言）锁定 v1.0.15 行为——checkNow 仅提示「后台自动下载更新」无手动按钮、updater:downloaded 出「立即重启/稍后」且点击调 updater:install、updater:error 兜底 openExternal、applyUpdate 走 updater:download 静默 IPC；全量门禁 85→**86 套件 0 失败**；commit `待提交`。
