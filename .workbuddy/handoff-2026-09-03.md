# 绿角犀 Office · 工作交接明细

> 交接日期：**2026-09-03** ｜ 版本：**1.0.16** ｜ 门禁：**90 套件 0 失败** ｜ 四端同源：**✅**
> 接手后第一件事：跑 `npm test` 取得本会话基线（约 1–2 分钟），再继续。
> 上一版交接：`.workbuddy/handoff-2026-08-19.md`（历史背景可查，本文为最新权威）。

---

## 0. 交接说明

本文用于**跨会话 / 跨人承接**。定位：只写「接手必须知道的现状 + 卡点 + 下一步」，不堆实现细节（细节在 FD 卡 / 模块总纲 / 日志）。

三个必读入口：

| 想看什么 | 去哪 |
|---|---|
| 项目范围 / 模块地图 | [工程总纲.md](工程总纲.md) |
| 当前状态 / 测试基线 | [STATUS.md](STATUS.md) |
| 版本变更史 | [CHANGELOG.md](CHANGELOG.md) |

---

## 1. 当前项目健康度（一句话）

**代码全部就绪、门禁全绿、四端同源；唯一缺口是「发布上传」与「git 推送」两项外部资源（需用户侧执行，沙箱网络不通）。**

| 指标 | 数值 |
|---|---|
| 版本 | `1.0.16`（package.json 单一真源） |
| 测试套件 | **90 套件 0 失败**（90 个 `_*_test.js` 文件） |
| 四端同源 | ✅ `npm run sync:check` 通过 |
| git | 41 commit · 17 tag（v1.0.0~v1.0.16）· **无 remote** |
| FD 卡 | 12 张（`FD/`） |
| PDF 模块 | 20 个 `pdf-*.js` 已加载进 `app/index.html` |
| 安装包产物 | `dist/` 四件套齐全（Setup 78.9MB / portable 78.7MB / .blockmap 83KB / latest.yml） |

---

## 2. 本轮（2026-08-19 → 2026-09-03）做了什么

按字母序推进 PDF 特性，每个都是「纯模块 + UI 接线 + 测试 + 门禁 + 文档 + 四端同步」11 步闭环：

| 标识 | 特性 | 模块 | 断言 | 门禁增量 |
|---|---|---|---|---|
| AH | PDF 链接/URI 提取 | `pdf-links.js` | 27 | 76→77 |
| AI | PDF 加密与权限检测 | `pdf-encrypt.js` | 43 | 78→79 |
| AJ | PDF 数字签名验证 | `pdf-signature.js` | 25 | 79→80 |
| AK | PDF 表单字段提取 | `pdf-formfields.js` | 34 | 81→82 |
| AL | PDF 文档信息/元数据 | `pdf-docinfo.js` | 33 | 82→83 |
| AM | PDF 页面属性/页面树 | `pdf-pageinfo.js` | 50 | 83→84 |
| AN | PDF 字体信息提取 | `pdf-fonts.js` | 74 | 84→85 |
| AO | PDF 1.5+ 对象流(ObjStm) 合并/拆分 | `pdf-tool.js` | 12 | 86→87 |
| AP | PDF 动作/JavaScript 安全审计 | `pdf-actions.js` | 37 | 87→88 |
| AQ | PDF 结构预检/完整性诊断 | `pdf-preflight.js` | 42 | 88→89 |
| AR | PDF 文档历史/增量更新审计 | `pdf-history.js` | 39 | 89→90 |

**非 PDF 的重大项**：登录设定（游客直进）、Windows 文件关联/默认打开、NSIS 铺盖体验、桌面端静默更新 → 全自动更新、Writer 功能区 P0/P1 补齐、FD 卡体系建立。

**PDF 只读审计族已收齐**：加密(AI) → 签名(AJ) → 动作(AP) → 字体(AN) → 结构(AQ) → 历史(AR)。

---

## 3. 工程现状（关键路径速查）

### 3.1 技术栈

- Web 真源：`app/`（**唯一权威**，其余端均为副本）
- 客户端：`clients/ios/`、`clients/harmonyos/`（webroot 镜像）、`clients/android/`（源码工程，未打包）
- 桌面：`electron/`（Electron + electron-updater）
- 服务端：`server/index.js`（Node 零依赖）
- 代码组织：IIFE + `OS` 全局 + `module.exports`（同一份文件浏览器/Node 双用，便于沙箱单测）

### 3.2 单一真源（不可破坏）

| 项 | 真源位置 |
|---|---|
| 版本号 | `package.json` → `node scripts/bump-version.js` 同步四端 |
| 前端代码 | `app/`（改完必须 `node scripts/sync-clients.js`） |
| 模块契约 | `OS.modules[type].mount(host, doc, ctx)`；文档模型 `OS.UOM`；存档 `.lvjx` |

### 3.3 测试与门禁（关键命令）

```bash
# 全量门禁（必须 exit 0）= node scripts/run-tests.js && npm run sync:check
npm test

# 只跑单测
node scripts/run-tests.js

# 四端同源检查（改过 app/ 必跑）
npm run sync:check

# 同步四端（改过 app/ 必跑，随后仍要补提 clients/*/index.html）
node scripts/sync-clients.js

# 单套件调试
node _pdf_history_test.js

# 语法快检
node --check app/js/modules/pdf-history.js
```

运行器自动发现根目录 `_*_test.js`，子进程隔离 + 超时。

### 3.4 常用脚本（`scripts/`）

| 脚本 | 用途 |
|---|---|
| `run-tests.js` | 全量测试运行器 |
| `sync-clients.js` | 四端 webroot 同步 + 同源检查 |
| `bump-version.js` | 版本单一真源同步 |
| `make-release.js` / `release-check.js` / `verify-release-assets.js` | 发布校验链 |
| `publish-desktop-feed.js` / `publish-latest.sh` / `publish-latest.bat` | 上传更新源到 VPS |

---

## 4. 完成度表（按模块）

| 模块 | 状态 | 说明 |
|---|---|---|
| 文档编辑（Writer/Spreadsheet/Presentation + MindMap） | ✅ 已实现+已验证 | Writer 功能区 P0/P1 已补齐 |
| PDF 全特性（阅读/批注/光栅/签名/文本层/表单/导出 + 解析族 AH~AR + ObjStm） | ✅ 已实现+已验证 | 20 个 pdf-* 模块 |
| 格式兼容（OOXML / OFD / docx→PDF） | ✅ 已实现+已验证 | PDF→DOCX 等为文本提取级 |
| 云端 + 单账号 + 商店 | ✅ 已实现+已验证 | 云后端零依赖自托管 |
| 客户端四端（iOS/HarmonyOS/Web/Desktop） | ✅ 同源 | Android 仅源码，无 SDK 未打包 |
| 桌面自动更新（v1.0.15 全自动模式） | ✅ 代码已验证 | **产物待上传 VPS 才真正生效** |
| Windows 文件关联 / 默认打开 | ✅ 已实现+已验证 | 注册表 + argv/second-instance → IPC |

---

## 5. 测试覆盖

- 根目录 **90 个** `_*_test.js`，全绿。
- 大型套件举例：PDF 批注统计 67 断言、AN 字体 74、AM 页面属性 50、注释导出 round-trip 50、AQ 预检 42、AI 加密 43、AK 表单 34、AL 文档信息 33、AJ 签名 25。
- 新增特性约定：**每个新模块 ≥12 断言**，实际都在 25–50 区间。

---

## 6. 残留缺口（按优先级）

### 6.1 ⛔ 阻断项 —— 必须用户侧执行（沙箱网络不通）

**① 发布 1.0.16 到 VPS（否则旧客户端收不到更新）**

- 动作：双击 `scripts/publish-latest.bat`（或 Git Bash 跑 `bash scripts/publish-latest.sh`）
- 上传目标：`https://lujax.fun/releases`（4 件套：Setup / portable / .blockmap / latest.yml）
- 耗时：约 1 分钟
- **沙箱为什么不能代做**：`lujax.fun` 解析 → VPS `8.149.245.252`，该 IP **全端口**在沙箱出口层被防火墙丢弃。已 6 探针实测：SSH 22 超时、HTTPS 443 超时、WebFetch fetch failed、r.jina.ai 中转超时、无系统代理可借道（WinHTTP 直连、注册表代理未启用）、出口基准正常（github 200 / baidu 200）。→ **定点丢包，非全局断网**。

**② git push（代码备份）**

- 现状：41 commit + 17 tag（v1.0.0~v1.0.16）本地就绪，`git remote -v` 为空。
- 动作：给一个 **github / gitee 等代理可达主机**的仓库 URL → `git remote add origin <url> && git push --tags && git push`
- 沙箱可推（github API 实测 200），只缺 URL。

### 6.2 🟡 边界（非阻断，已知降级）

- **PDF→DOCX / TXT / MD / Excel(CSV)**：文本提取级还原，非像素级版面还原。
- **Android APK**：本环境无 Android SDK / gradle，未打包、未伪造；`clients/android/app/build` 已移出 git 索引（`.gitignore` 兜底）。
- **iOS / HarmonyOS**：仅源码工程，需苹果/华为开发者环境才能出包。

### 6.3 ⚠️ 已记录非阻断

- `_auth_backup_test` 并行时偶发资源争用抖动（隔离跑稳定，非阻断）。
- 真实 Canvas / pdf.js DOM 集成路径在沙箱无运行环境，渲染/交互依赖目检。
- 云后端仅自托管单机，公网部署 / 负载均衡未规划。

---

## 7. 关键工程纪律（不可破坏）

1. **Trust-but-Verify**：不靠「已创建 / 已修复」判定完成，必须回查产物（跑测试、grep 落盘、读文件）。
2. **严格忠实镜像**：iOS / HarmonyOS webroot 必须与 `app/` 一致；改完 `sync-clients` 后仍要检查 `clients/*/index.html` 是否带上了新 script 标签（**已连续 3 轮需要补提**，AP/AQ/AR 都中招）。
3. **版本单一真源**：只改 `package.json`，再 `bump-version.js`。
4. **测试诚实**：失败先诊断根因，禁 sleep 重试循环。
5. **零依赖优先**：沙箱可测的纯逻辑抽独立模块（IIFE + OS 全局 + module.exports）。
6. **图标不可臆造**：新增 ribbon 按钮前先 `grep app/js/icons.js`。
7. **文档回写**：每轮更新 STATUS / FEATURES / 对应模块总纲 / FD 卡 / 当日日志。

---

## 8. 高频踩坑（PDF 字节级解析，跨特性复用）

> 完整版见 `.workbuddy/memory/MEMORY.md`，此处只列最容易再犯的。

- **合成测试 PDF 必须带 `trailer << /Root N 0 R >>`**：`findRootDict` 锚 `/Root` 正则，缺 trailer 则全表挂。
- **`sliceDict` 族闭合要含末位 `>`**：`slice(startIdx, i + 1)`，写成 `i` 会丢末位 `>`，嵌套字典（如 `/Names` 三层）必失衡。6 模块已修，勿退回旧写法。
- **ObjStm 合成偏移表必须 `num off` 严格交错**（`"2 0 3 38 4 84 "`）：错一位 `parseObjStm` 会把 offset 当对象号 → 幻影对象。
- **startxref 偏移用 `pdf.length` 实时计算**，手算必错位。
- **`/Prev` 链是增量更新回溯的权威依据**；`readTrailerAt` 双路径（`trailer` 关键字优先，找不到再按 `/Type /XRef` 取 xref 流对象字典）。
- **`OS.PdfTool.parsePdf` 页面树 walk 无环保护**：/Kids 环会栈溢出；做页面树遍历的模块须自带 visited 安全走树（AQ 已内置，勿改为依赖 `parsePdf.pages`）。
- **字面串分词**：`(` 起 depth=1，`)` 使 depth≤0 即停；操作符词按空白/特殊字符截断（`isNumCh` 含 e/E 会切出孤立 e → OOM）。
- **内容流解释用操作数栈**（操作符从栈顶弹），不能前向贪婪 `eatNum()`。
- **引用正则 `/(\d+)\s+\d+\s+R/` 捕的是对象号**，不是代数；`getObjDict` 须锚定 `num+"\s+0\s+obj\s*<<"`。
- **Edit 工具幻影写入**（会话级现象，已多次实测）：回执成功但磁盘未变 —— **每次 Edit 后必须 grep / Read 回查**；批量改动优先整文件 `Write` 原子重写。
- **构建通道坑**：用 PowerShell 后台包装起 electron-builder 会被回收成僵尸（进程亡、无产物）；改用 **Bash 后台通道**。构建命令：`NODE_OPTIONS="" npx electron-builder --win --publish never`（退 0，约 30s）。

---

## 9. 下一步推进建议（按可验证 / 可交付优先序）

1. **① 发布 1.0.16**（用户侧 1 分钟）→ 旧客户端即可收到自动更新。**这是唯一影响交付的硬动作。**
2. **② 提供 git remote URL**（10 秒）→ 推 41 commit + 17 tag，代码有异地备份。
3. **继续字母序特性 AS 起**：PDF 审计族已收齐，AS 起应开新方向（建议：PDF 内容层新能力，或转向 Writer/Presentation 深度功能）。
4. **发版收口**：AP/AQ/AR 已提交未发版，下次 bump 到 1.0.17 时随包走（记得重建 dist + 重传 VPS）。

---

## 10. 关键文件索引

```
app/                               # Web 真源（唯一权威）
├── index.html                     # 加载所有 module 脚本
├── js/
│   ├── icons.js                   # 图标库（新增按钮前必 grep）
│   ├── pdf-tool.js                # OS.PdfTool 解析原语（parsePdf/inflate/parseObjStm）
│   └── modules/
│       ├── pdf.js                 # PDF 主模块（ribbon 分组 + 各 show* 弹窗）
│       ├── pdf-actions.js         # AP 动作/JS 安全审计
│       ├── pdf-preflight.js       # AQ 结构预检（异步）
│       ├── pdf-history.js         # AR 文档历史（同步）
│       └── pdf-*.js               # 其余 17 个 PDF 子模块
clients/
├── ios/webroot/                   # 镜像（sync-clients 生成）
├── harmonyos/entry/.../rawfile/   # 镜像（sync-clients 生成）
└── android/                       # 源码工程（无 SDK，未打包）
electron/
├── main.js                        # 自动更新（autoDownload / autoInstallOnAppQuit）
└── feed-config.js                 # generic provider 解析（lujax.fun/releases）
server/index.js                    # 云后端（零依赖）
scripts/                           # 构建 / 测试 / 同步 / 发布脚本
FD/                                # 功能开发记录卡（12 张）
.workbuddy/
├── memory/                        # 按日日志 + 长期 MEMORY.md
├── handoff-2026-08-19.md          # 历史交接
└── handoff-2026-09-03.md          # 本文件（最新）
```

---

## 11. 备注 / 风险

- **沙箱网络限制（铁定，2026-09-02 六探针实测）**：沙箱出口仅代理型 HTTP(S) 可用；VPS `8.149.245.252` 全端口在 IP 层被丢。故指向该 VPS 的发布 / 上传 / SSH 在沙箱内**物理不可行**。私钥 `~/.ssh/id_ed25519_rcprod` 在沙箱但用不上。
- **API 频率限制**：长连续轮次会触发限流（429）；建议工作分段，接手先跑 `npm test` 取基线。
- **签名证书 / 隐私页 URL / 自托管 Runner 凭证**：纯上架人工项，代码未缺失，非阻断。
- **桌面 Mac/Linux 构建**：CI 已绿，但本机仅 Windows 可实跑，依赖 CI 产物。

---

> 交接完毕。接手方首选：先确认 §6.1 两项是否已由用户完成；未完成则优先催办 ①（唯一影响交付）；完成后按 §9 第 3 项继续字母序开发。
