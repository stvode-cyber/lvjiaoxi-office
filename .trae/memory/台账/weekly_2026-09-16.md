# 📋 台账周报 · 2026-09-08 ~ 2026-09-15

**项目**：lvjiaoxi-office  **版本**：1.1.1
**测试**：96 passed / 0 failed / 96 套件
**铁律**：10 条  **活跃坑**：10 条  **决策**：7 条

---

## 一、模块状态

| 模块 | 状态 | 备注 |
|------|------|------|
| Writer 富文本 | ✅ 可用 | 基础编辑 + 导出 docx/html/md |
| Sheet 表格 | ✅ 可用 | AI 公式转换（SUM/AVG/MAX/MIN/IF/RANK） |
| Presentation 幻灯片 | ✅ 可用 | AI 大纲生成 + 导入当前文档 + 5 段式结构 |
| Mindmap 思维导图 | ✅ 可用 | XMind/MD/OPML 导入 + 递归布局 + 7 色循环 |
| PDF 工具箱 | ✅ 可用 | 深色主题局部作用域 |
| AI 面板（BYOK） | ✅ 可用 | OpenAI/Claude/DeepSeek/Gemini/自定义，密钥 localStorage |
| 历史版本快照 | ✅ 可用 | IndexedDB v3 + LCS Diff + 自动 30s/2s debounce |
| XMind 导入 | ✅ 可用 | JSZip 解 content.json + 根节点 #1e3a5f |

## 二、测试结果

✅ **96/96 全绿** — 无红单

## 三、活跃坑分布

| 严重度 | 数量 | 说明 |
|--------|------|------|
| P0 | 2 | 阻断发布 |
| P1 | 7 | 影响核心体验 |
| P2 | 1 | 影响局部 |
| P3 | 0 | 不爽但能用 |

## 四、本周新修复（10 条）

### P1 · PDF 模块 escapeHtml 转发 OS.util 在 Node 环境 undefined（2026-09-15）

- **根因**：浏览器 util.js 先加载、PDF 模块后运行所以 OK；但 Node 单测直接 require 模块时 util.js 未注入全局 → `OS.util` 为 undefined
- **解决**：每个 PDF 模块 escapeHtml 加 `global.OS?.util?.escapeHtml` 存在性守卫 + 内置 fallback 实现
- **预防**：任何"转发到 OS.*"的工具函数必须做守卫 + 本地 fallback，禁止假设加载顺序
- **关联**：app/js/modules/pdf-{preflight,docinfo,anno-timeline,encrypt,formfields,history,links,signature}.js

### P1 · shell.js `_on` 定义在 renderTopPanel 内部但被外层函数调用（2026-09-15）

- **根因**：renderTopPanel 重构时在内部声明 `const _on` 做局部事件管理，但忘了删除其他地方对同名变量的旧引用；浏览器非严格模式可能 silent fail 或隐式挂到 window
- **解决**：将 `_Listeners` + `_on` + `_offAll` 从 renderTopPanel 内部提升到 shell.js IIFE 顶层
- **预防**：IIFE 内共享工具函数/变量必须声明在顶层；重构时 grep 扫同名引用
- **关联**：app/js/shell.js（Line 17-20 新增顶层声明，原 Line 40 局部声明已删除）

### P0 · koa-connect wrapper 导致 ctx 泄漏（2026-09-15）

- **根因**：koa-connect 内部代理层对 request 对象做了浅拷贝，流式 body 在拷贝后丢失
- **解决**：重写为原生 Koa middleware，不走 wrapper
- **预防**：所有 Koa 路由禁止使用 koa-connect；新写中间件一律原生 Koa 风格
- **关联**：electron/main.js / server/index.js / scripts/*.js

### P1 · XMind 导入 mkNode 默认颜色 bug（2026-09-15）

- **根因**：XMind content.json 没 theme 字段时，渲染逻辑走到了 `undefined || fallback` 但 fallback 漏写
- **解决**：给 mkNode 默认值 `#2563eb` 并在 importXmind 中显式传 color 参数，根节点用 `#1e3a5f`
- **预防**：所有渲染函数默认值必须显式硬编码，禁止"依赖上游总会传"；解析 JSON 前先判空
- **关联**：app/js/modules/mindmap.js

### P0 · electron-builder CSC_LINK 空指针崩溃（2026-09-15）

- **根因**：CI 脚本假设所有环境都有证书，开发机本地构建时环境变量缺失
- **解决**：加 `if: env.CSC_LINK` 条件，空则不注入证书变量
- **预防**：所有 secrets 环境变量必须 `if: env.XXX` 守卫，禁止裸引用
- **关联**：electron/main.js / electron/*.js

### P1 · 启动时 OS 对象 undefined（2026-09-15）

- **根因**：异步加载顺序不确定，AI 面板 DOM 渲染早于 OS store 初始化
- **解决**：在 renderer 入口加 `await OS.ready()`，所有子模块 mount 完成才 resolve
- **预防**：所有跨模块调用前先检查 `OS?.ready`；模块 mount 用 Promise 链而非裸 setTimeout
- **关联**：app/js/auth.js / app/js/store.js

### P1 · Toast 容器未挂载到 body（2026-09-15）

- **根因**：toastContainer 只 render 到 Virtual DOM 没真正 commit
- **解决**：App 初始化时强制 document.body.appendChild(toastContainer)
- **预防**：全局组件（Toast/Tooltip/Modal）必须在 App mount 阶段主动 appendChild 到 body
- **关联**：app/js/util.js / app/js/shell.js

### P2 · PowerShell 正则引号地狱（2026-09-15）

- **根因**：PowerShell 5 引号转义规则与 bash/Python 完全不同，跨平台脚本必炸
- **解决**：改走 Node 脚本执行审计逻辑，PowerShell 只做外壳
- **预防**：复杂正则 / 文件扫描一律写 Node 脚本，PowerShell 只负责调用
- **关联**：scripts/run-tests.js / scripts/*.js

### P1 · fileAssoc 硬编码绝对路径跨环境炸（2026-09-15）

- **根因**：开发机本地调试时偷懒写死
- **解决**：全部换成 `__dirname` 相对路径
- **预防**：禁止任何源码出现 `C:\` / `/home/` 等绝对路径；grep 扫 `[A-Z]:\\[a-z]` 拦截
- **关联**：app/js/**/*.js / scripts/*.js

### P1 · 订单/库存/审批残留引用导致白屏（2026-09-15）

- **根因**：删代码只删看得见的 UI，没扫下游依赖链
- **解决**：全局 grep `order|inventory|approval`，命中文件全部清理
- **预防**：功能下线流程 = UI 删除 + JS 模块删除 + CSS 清理 + 事件解绑 + 路由移除 + grep 验证 六步走
- **关联**：app/js/shell.js / app/index.html / app/css/*.css

## 五、台账变更（6 条）

- [2026-09-15] 台账系统初始化：decisions/issues/context/index 四文件落地
- [2026-09-15] Mindmap 新增 XMind/MD/OPML 导入，mkNode 默认色 bug 已修
- [2026-09-15] 历史版本快照 + LCS Diff 上线
- [2026-09-15] 台账系统联调：OS.ready() Promise 机制落地（auth.js + store.js）；6 处源码注入预防 TODO 注释；8 坑扫描 5 已修复 2 旧预防注释已就位 1 新注入
- [2026-09-15] 8 红单全修 → 96/96 绿（7 个 PDF escapeHtml + 1 个 shell _on 作用域）
- [2026-09-15] 台账自动化脚本 `scripts/ledger-precheck.js` 落地：改前扫坑 + 输出 TODO 建议 + exit 1 阻断 CI；issues.md 新增 2 条坑（含快速排查清单）

## 六、铁律速查（10 条）

1. **版本号四端强制同步**：源码常量 / UI 显示 / 安装包属性 / 更新通道，一字不差
1. **Git 走 HTTPS + PAT**：SSH 禁；PAT 存 `C:\Users\Administrator\.lvjiaoxi-git-credentials`
1. **BYOK 密钥仅 localStorage**：不上云不进仓库
1. **深色主题局部作用域**：PDF 用 `.pdf-toolbox-root`，别污染全局浅色 token
1. **Electron CI secrets 必须 if guard**：`if: env.CSC_LINK`，空则不注入
1. **路径一律相对**：源码禁 `C:\` 绝对路径，测试用 `__dirname`
1. **AI 面板嵌入工具栏**：GenOffice 模式，禁止外挂聊天框
1. **功能下线六步走**：UI 删 → JS 删 → CSS 清 → 事件解绑 → 路由移除 → grep 验证
1. **OS 跨模块调用先 ready**：`await OS.ready()` 再做事
1. **Koa 路由禁 koa-connect wrapper**：一律原生 Koa middleware

## 七、技术积累（从坑根因自动提炼）

1. **PDF 模块 escapeHtml 转发 OS.util 在 Node 环境 undefined** — 浏览器 util.js 先加载、PDF 模块后运行所以 OK；但 Node 单测直接 require 模块时 util.js 未注入全局 → `OS.util` 为 undefined
2. **shell.js `_on` 定义在 renderTopPanel 内部但被外层函数调用** — renderTopPanel 重构时在内部声明 `const _on` 做局部事件管理，但忘了删除其他地方对同名变量的旧引用；浏览器非严格模式可能 silent fail 或隐式挂到 window
3. **koa-connect wrapper 导致 ctx 泄漏** — koa-connect 内部代理层对 request 对象做了浅拷贝，流式 body 在拷贝后丢失
4. **XMind 导入 mkNode 默认颜色 bug** — XMind content.json 没 theme 字段时，渲染逻辑走到了 `undefined || fallback` 但 fallback 漏写
5. **electron-builder CSC_LINK 空指针崩溃** — CI 脚本假设所有环境都有证书，开发机本地构建时环境变量缺失
6. **启动时 OS 对象 undefined** — 异步加载顺序不确定，AI 面板 DOM 渲染早于 OS store 初始化
7. **Toast 容器未挂载到 body** — toastContainer 只 render 到 Virtual DOM 没真正 commit
8. **PowerShell 正则引号地狱** — PowerShell 5 引号转义规则与 bash/Python 完全不同，跨平台脚本必炸
9. **fileAssoc 硬编码绝对路径跨环境炸** — 开发机本地调试时偷懒写死
10. **订单/库存/审批残留引用导致白屏** — 删代码只删看得见的 UI，没扫下游依赖链

## 八、指标汇总

| 指标 | 值 |
|------|----|
| 测试套件 | 96 |
| 活跃坑 | 10 |
| 关键决策 | 7 |
| 铁律 | 10 |
| 模块 | 8 |
| 台账变更（周期内） | 6 |

> 本周报 100% 自动生成，数据源：issues.md + decisions.md + context.md + npm test 实跑。
> 下次生成：`node scripts/ledger-weekly.js [--no-save] [--weeks N]`
