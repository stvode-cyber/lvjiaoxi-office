# 工程总纲 · 绿角犀 Office

> 本文件是项目级**总纲（Master Outline）**，只负责「指路 + 状态 + 关系」。
> 具体实现细节、剧情明细不在本文件展开，统一下沉到各模块总纲与子文件。
> 本文件为范围与导航的**单一真源（Single Source of Truth）**。

---

## 一、项目目的

交付「绿角犀 Office」跨平台办公套件：**Writer / Spreadsheet / Presentation / PDF / MindMap**。

- Web（`app/`）为**单一真源**，四端原生壳**同源承载**（严格忠实镜像、零漂移）。
- 打通四平台发布闭环：**构建 → 自检 → 发版 → 静默更新**。

---

## 二、文档治理规范（强制，全仓库遵守）

1. **职责单一**：每个大模块职责单一；同一资料不在多目录重复维护（唯一真源原则）。
2. **主目录只导航**：根目录只放导航文件，不堆大量正文；具体内容下沉到模块子文件。
3. **模块必带总纲**：每个模块必须有自己的「总纲」，负责四件事 ——
   - 模块目的
   - 包含哪些子文件
   - 当前状态
   - 与其他模块关系
4. **工作顺序（先总纲后细节）**：
   `工程总纲 → 模块总纲 → 子模块资料 → 具体内容`，不直接进入细节开发。
5. **讨论与正式分离**：推演、争议、未定方案只在 `讨论与处理/`；确认后**回写**对应模块，**不污染**正式资料。
6. **导航文件只指路**：`AGENTS` / `STATUS` / `MEMORY` / `CHANGELOG` / 各模块总纲 只负责「指路 + 状态管理」，不保存大量剧情明细。

---

## 三、模块地图（导航）

| 编号 | 模块 | 总纲 | 职责 |
|------|------|------|------|
| 01 | 文档编辑 | [模块/01_文档编辑_总纲.md](模块/01_文档编辑_总纲.md) | Writer / Spreadsheet / Presentation / MindMap 核心编辑 |
| 02 | PDF | [模块/02_PDF_总纲.md](模块/02_PDF_总纲.md) | 阅读 / 合并拆分 / 批注 / 签名 / 文本层 / 表单 / 转换 |
| 03 | 格式兼容 | [模块/03_格式兼容_总纲.md](模块/03_格式兼容_总纲.md) | OOXML / ODF / OFD 导入导出、多格式打开 |
| 04 | 账户与云端 | [模块/04_账户与云端_总纲.md](模块/04_账户与云端_总纲.md) | 统一账号 / 云端后端 / 配额 / 备档 / .lvjx / 加密 |
| 05 | 外壳与通用 | [模块/05_外壳与通用_总纲.md](模块/05_外壳与通用_总纲.md) | 开始页 / ribbon / 命令面板 / 主题 / 撤销 / AI 引擎 |
| 06 | 客户端与交付 | [模块/06_客户端与交付_总纲.md](模块/06_客户端与交付_总纲.md) | electron / android / ios / harmonyos / pwa |
| 07 | 发版与更新 | [模块/07_发版与更新_总纲.md](模块/07_发版与更新_总纲.md) | scripts 构建 / 发版 / 更新 / 商店合规 |
| 08 | 测试与质量 | [模块/08_测试与质量_总纲.md](模块/08_测试与质量_总纲.md) | 测试套件 / 运行器 / CI / 覆盖率 |

---

## 四、技术栈与约定

- **Web 真源**：`app/`，零构建零依赖纯静态（经典 `<script>` + 全局 `OS.*` 命名空间，无打包器）。
- **客户端壳**：`electron/`（桌面 Win/Mac/Linux）、`clients/{android,ios,harmonyos}`（原生 Web 组件）；由 `scripts/sync-clients.js` **严格镜像** `app/`，改后必同步、零漂移。
- **发版/校验**：`scripts/` Node 脚本 + GitHub Actions CI；Node 20/22 运行时。
- **服务端**：`server/index.js` 零依赖 Node 云后端（scrypt + HMAC token）。
- **版本单一真源**：仅 `package.json`，由脚本注入四端。
- **测试发现**：根目录 `_*_test.js`；`npm test` = `node scripts/run-tests.js && npm run sync:check`。
- **文档模型**：`OS.UOM`；存档 `.lvjx`；自动备档每 5 分钟。

### 合规约束

- 中国地图 / 领土表述按官方口径（HK/Macau/Taiwan 为中国一部分）。
- PRD 要求登录/账户能力**全平台必备**、**禁止静默失败**。

---

## 五、当前状态摘要（详见 [STATUS.md](STATUS.md)）

- **基线**：`npm test` 整链 **43 套件 0 失败** + 四端同源 ✅
- 🔴 **大型未实现**：PDF→Excel、docx→PDF 程序化、MindMap→OOXML/OFD
- 🟡 **边界进行中**：PDF→DOCX/TXT/MD（版面还原有限）

---

## 六、模块间关系

```
                ┌─────────────┐
                │   工程总纲    │ (导航/状态/关系)
                └──────┬──────┘
        ┌──────────────┼───────────────────────┐
        │              │                        │
   ┌────┴────┐   ┌─────┴──────┐          ┌──────┴──────┐
   │ 01 文档  │──▶│ 03 格式兼容 │◀────────│ 02   PDF     │
   │   编辑   │   │ (依赖UOM)  │          │ (导出DOCX)  │
   └────┬────┘   └─────┬──────┘          └──────┬──────┘
        │              │                         │
        └──────┬───────┴──────────┬──────────────┘
               │                   │
         ┌─────┴─────┐       ┌──────┴──────┐
         │ 05 外壳通用 │──────▶│ 04 账户云端  │
         │ (账户抽屉) │       │ (同步/备档) │
         └─────┬─────┘       └──────┬──────┘
               │                    │
         ┌─────┴─────┐       ┌──────┴──────┐
         │ 06 客户端  │◀──────│ 07 发版更新  │
         │ (镜像app) │       │ (scripts)   │
         └─────┬─────┘       └──────┬──────┘
               └─────────┬──────────┘
                     ┌────┴────┐
                     │ 08 测试  │ (覆盖全部模块)
                     └─────────┘
```

- **格式兼容（03）** 依赖 **文档编辑（01）** 的 `OS.UOM` 模型做导出/往返。
- **PDF（02）** 的导出 DOCX 复用 **格式兼容（03）** 的 `OS.Exporter.buildDocx`。
- **账户云端（04）** 依赖 **外壳通用（05）** 的账户抽屉入口，提供同步/备档。
- **客户端（06）** 由 `sync-clients` 严格镜像 **app/** 真源；**发版更新（07）** 消费四端工程与 `scripts/`。
- **测试质量（08）** 横向覆盖全部模块，是完成定义的客观证据来源。

---

## 七、导航文件索引

| 文件 | 职责（只指路 + 状态） |
|------|------|
| [AGENTS.md](AGENTS.md) | 自主运作宪法（循环 / DoD / 验证纪律）+ 本文件指针 |
| [STATUS.md](STATUS.md) | 状态-only，指向各模块总纲与功能完成度 |
| [MEMORY.md](MEMORY.md) | 长期决策，导航-only（详细落 `.workbuddy/memory/`） |
| [CHANGELOG.md](CHANGELOG.md) | 变更日志，导航-only |
| [讨论与处理/](讨论与处理/) | 讨论与推演，分离于正式资料 |
| [模块/](模块/) | 各模块总纲，单一职责、各管各的目的/子文件/状态/关系 |

---

> 维护规则：本文件只随「项目范围 / 模块划分 / 全局约定」变化而更新；任何功能细节请下沉到对应模块总纲与子文件。
