# 模块 08 · 测试与质量 总纲

> 模块级总纲：只负责「模块目的 / 包含子文件 / 当前状态 / 与其他模块关系」。
> 具体实现细节下沉到子文件（本轮仅规划，见 §二）。

## 一、模块目的

为完成定义（DoD）提供**客观证据**：测试运行器、回归套件、CI、覆盖率。
主命令 `npm test` = `node scripts/run-tests.js && npm run sync:check`。

## 二、包含子文件

| 子文件 | 内容 | 状态 |
|--------|------|------|
| `runner.md` | 测试运行器（自动发现 `_*_test.js`、子进程隔离+超时） | 规划中（待建） |
| `suites.md` | 回归套件分组（按模块），断言清单 | 规划中（待建） |
| `ci.md` | CI 入口 `.github/workflows/{tests,client-sync,release,release-desktop}.yml` | 规划中（待建） |
| `coverage.md` | 覆盖率与遗留缺口（缺完整 E2E） | 规划中（待建） |

## 三、当前状态

| 子能力 | 状态 | 说明 |
|--------|------|------|
| 测试运行器 | ✅ 已实现 | 自动发现 `_*_test.js`，子进程隔离+超时 |
| 回归套件 | ✅ 已实现 | 43 套件 0 失败 |
| 集成断言 | ✅ 已实现 | 云后端 25 + 前端同步 16 + AI 代理 12 + 账号回归 + PDF 批注 31 + 光栅 14 + 签名 17 + 文本索引 28 + 表单 24 + OOXML 图片 14 + PDF 文本提取 19 |
| CI | ✅ 已实现 | `.github/workflows/{tests,client-sync,release,release-desktop}.yml` |
| 完整 E2E | 🟡 缺口 | 导出/登录/云同步已有集成断言，缺端到端全链路 |

> 注意：`_auth_backup_test` 偶发失败属并行子进程资源争用，隔离稳定，非阻断。

## 四、与其他模块关系

- **横向覆盖 01–07 全部模块**：是 DoD「构建/测试全绿」「回查实际产物」的证据来源。
- **门禁 → 发版与更新（07）**：发版前须 `npm test` 全绿 + `sync:check` 同源校验。
- **依据 → 工程总纲** 验证纪律（Trust-but-Verify）。

## 五、具体内容入口

详见 §二 子文件（待建）。断言基线数字以每次 `npm test` 实跑为准，禁止靠「已创建」判定完成。
