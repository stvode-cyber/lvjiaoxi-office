# 模块 05 · 外壳与通用 总纲

> 模块级总纲：只负责「模块目的 / 包含子文件 / 当前状态 / 与其他模块关系」。
> 具体实现细节下沉到子文件（本轮仅规划，见 §二）。

## 一、模块目的

套件级外壳与通用能力：**开始页 / 多标签 ribbon / 命令面板 / 主题 / 撤销重做保存 / AI 引擎**。
为各编辑模块（01/02）与账户抽屉（04）提供统一宿主与入口。

## 二、包含子文件

| 子文件 | 内容 | 状态 |
|--------|------|------|
| `startpage.md` | 开始页（新建/打开/最近） | 规划中（待建） |
| `ribbon.md` | 多标签 + 动态功能区 ribbon | 规划中（待建） |
| `command-palette.md` | 命令面板（Ctrl/Cmd+K） | 规划中（待建） |
| `theme.md` | 主题切换（亮/暗） | 规划中（待建） |
| `history.md` | 撤销/重做、保存 | 规划中（待建） |
| `ai-engine.md` | AI 引擎（本地离线 + 云端可选，`OS.AI`） | 规划中（待建） |

## 三、当前状态

| 子能力 | 状态 | 说明 |
|--------|------|------|
| 开始页 | ✅ 已实现 | 新建/打开/最近 |
| 多标签 + 动态 ribbon | ✅ 已实现 | 多标签 + 动态功能区 |
| 命令面板 | ✅ 已实现 | Ctrl/Cmd+K |
| 主题切换 | ✅ 已实现 | 亮/暗 |
| 撤销/重做、保存 | ✅ 已实现 | — |
| AI 引擎 | ✅ 已实现 | 本地离线指令链（总结/润色/改写/扩写/续写/大纲/翻译/解释/审阅）；云端走 OpenAI 风格流式（服务端代理或自有 endpoint），未配置自动回退本地，绝不静默失败 |

## 四、与其他模块关系

- **宿主 → 文档编辑（01）/ PDF（02）**：ribbon、命令面板、撤销/重做在此挂载各模块能力。
- **宿主 → 账户与云端（04）**：账户抽屉入口设于外壳。
- **宿主 → 发版更新（07）**：后台更新提示经外壳展示。
- **被 测试质量（08）** 覆盖（AI 代理 12 断言等）。

## 五、具体内容入口

详见 §二 子文件（待建）。
